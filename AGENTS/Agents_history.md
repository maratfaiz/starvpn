# Agents_history.md — история работы агентов

Здесь каждый агент оставляет запись **после** выполнения задачи: кто он
(роль), что сделал, что важно знать следующему. Это не commit-лог (он и
так есть в git) — это человеческое, читаемое резюме "что происходило и
почему", плюс передача контекста между агентами.

**Новую запись добавляй в самый низ файла.** Не редактируй чужие записи —
если что-то нужно уточнить, добавь свою запись со ссылкой на старую.

---

## Формат записи

```markdown
### YYYY-MM-DD — <короткое имя задачи>

- **Роль:** <дизайнер / frontend / backend / bot / database / devops / qa>
- **Кто:** <модель/агент, если известно>
- **Что сделал:** 2–5 пунктов по сути
- **Затронутые файлы/папки:** список
- **Важно для следующего агента:** предупреждения, недоделанное, вопросы к человеку
```

---

## Записи

### 2026-08-23 — Пересборка сайта под новый дизайн-экспорт + бэкенд-вайринг

- **Роль:** frontend + designer + немного backend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - `landing/` и `admin/` были удалены прямо перед началом работы —
 восстановлены и пересобраны под новый Claude Design export ("STAR VPN").
 - Главная, Тарифы, Wiki (индекс + 11 статей, включая 4 новых гайда по
 установке), Личный кабинет, Вход, Поддержка, Privacy/Terms, get-vpn —
 приведены к новому макету и подключены к уже существующему backend API.
 - `bot/api.py`: расширен список Wiki-слагов, `/connect` → 301 redirect
 на `/wiki`, `/api/gift/pending` и `/api/gift/seen` переведены на общий
 резолвер identity (`_resolve_tg_id`), чтобы работать и для веб-аккаунтов.
 - Админка (`admin/index.html`) — уже была полностью рабочей и почти
 точно соответствовала новому макету; переделан только экран входа
 (визуально), логика входа (единый `ADMIN_WEB_KEY` через `/web/login`)
 оставлена как есть — макет предполагал email+пароль+2FA, которых в
 реальном бэкенде нет.
- **Затронутые файлы/папки:** `landing/**`, `admin/index.html`, `bot/api.py`
- **Важно для следующего агента:**
 - Флагнутые несостыковки макета с реальностью (не исправлены молча,
 решение оставлено как есть — см. PR-описание в git log): (1) вход в
 админку в макете — email+пароль+2FA, в реальности — один общий ключ;
 (2) карточка "локации серверов" в личном кабинете в макете показывает
 5 живых серверов с пингом, по факту работает один (Амстердам); (3)
 "Ссылка подписки" и flow подарка в макете — вымышленные эндпоинты,
 реализован вариант на основе реальных `/api/gift/lookup` → `/api/gift/invoice`.
 - Полный деплой на прод (`starvpnservice.ru`, VPS на firstvds.ru) занял
 отдельную длинную сессию — см. следующую запись.

### 2026-08-30 — Первый боевой деплой на VPS + структура AGENTS/

- **Роль:** devops
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - Задеплоил ветку `original` (бывшая `claude/implement-claude-design-1ax5l7`)
 на прод-сервер (`178.250.157.113`, домен `starvpnservice.ru`, front —
 Netlify proxy → порт 8080 сервера).
 - Починил `.env`: не было `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB`,
 из-за чего Postgres не мог инициализироваться (`Database is
 uninitialized and superuser password is not specified`).
 - **Важная находка:** `docker compose` берёт `.env` для подстановки
 `${VAR}` в `docker-compose.yml` из **той же папки, где лежит сам
 `docker-compose.yml`** (`infra/.env`), а не из корневого `.env`,
 который читается только через `env_file: ../.env` у сервиса `bot`.
 Пришлось сделать `cp /opt/starvpn/.env /opt/starvpn/infra/.env` —
 **если меняешь корневой `.env` на сервере, не забудь повторить копию**
 (см. `AGENTS/roles/devops.md`).
 - Переименовал GitHub-ветку `claude/implement-claude-design-1ax5l7` →
 `original` и сделал её default-веткой репозитория; удалил старую
 неиспользуемую ветку `claude/file-upload-location-ot9fqv` (была
 идентична общей точке ветвления, без уникальных коммитов).
 - Убрал иконку-логотип рядом с текстом "STAR VPN" везде на сайте,
 усилил звёздный фон на Главной/Тарифах/Wiki (был технически на
 месте, но еле заметен — увеличена плотность и яркость, единый
 тёплый золотой оттенок).
 - Создал структуру `AGENTS/` (этот файл и всё, что рядом), почистил
 "мусорные" файлы: старый `deploy.sh` в корне (личный rsync-скрипт с
 чужим сервером/путями), устаревшую `bot/migrations/*.sql` (заменена
 Alembic-миграциями и ничем в коде не используется), забытый
 Vite-артефакт `webapp/vite.config.js.timestamp-*.mjs`.
- **Затронутые файлы/папки:** `.env`/`infra/.env` (на сервере, не в git),
 `bot/api.py`, `landing/*.html`, `admin/index.html`, `AGENTS/**`,
 `.gitignore`, удалены `deploy.sh` (корень), `bot/migrations/`
- **Важно для следующего агента:**
 - На сервере всё ещё существует локальная ветка `main` с тремя
 коммитами от стороннего агента (похоже, ChatGPT, работавшего
 напрямую на сервере в это же время): `fix(ui): replace emoji with
 SVG icons across site + admin`, `fix(wiki): restore missing
 Подключение category on Wiki index`, `docs: add README explaining
 repo layout and architecture`. **Эти коммиты нигде не запушены на
 GitHub** — если в них было что-то ценное (например, замена эмодзи на
 SVG-иконки), это стоит перенести в основную ветку `original`
 осознанно, а не потерять молча.
 - SSH с рабочего компьютера пользователя на сервер стабильно не
 работает (`Connection closed`/`refused` даже с мобильного
 интернета) — похоже на фильтрацию на уровне связи, не на сервере
 (ufw/fail2ban/csf на сервере ничего не блокируют). Деплой велся
 через веб-консоль (VNC) хостинга — там нет copy-paste, все команды
 вводились вручную, отсюда риск опечаток при повторном деплое.
 - Пользователь просил проверить/установить инструмент под названием
 "graphify" — по итогу оказалось, что это не существующий
 инструмент/плагин; если он снова всплывёт — уточнить, что именно
 имеется в виду (вероятно, имелась в виду Grafana).

### 2026-08-30 — Вход в личный кабинет: magic-link → email+пароль

- **Роль:** backend + frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - Полностью заменил passwordless-вход (magic-link на email) на
 обычный email+пароль с подтверждением почты кодом — см. ADR-006 в
 `AGENTS/decisions/ADR.md` для полного контекста решения.
 - `users` получил `password_hash`/`email_verified`; новая таблица
 `email_verification_codes` (хеш кода, попытки, срок действия) —
 миграция `0008_password_auth`. Старая `magic_link_tokens` и модель
 `MagicLinkToken` удалены (`bot/models/magic_link.py` удалён,
 миграция `0009_drop_magic_link_tokens`).
 - `bot/utils/webauth.py`: `hash_password`/`verify_password` (bcrypt),
 `create_verification_code`/`check_verification_code` (SHA-256 хеш
 кода, cooldown 45с на переотправку, лимит 5 попыток), `register_user`.
 - `bot/api.py`: новые `POST /api/account/register`,
 `POST /api/account/resend-code`, `POST /api/account/verify-email`;
 `POST /api/account/login` теперь принимает `{email, password}` вместо
 только `{email}`. `GET /account/verify` (старый приёмник magic-link)
 удалён.
 - `landing/login.html` переписан: вкладки "Вход"/"Регистрация", поле
 пароля, отдельный экран ввода 6-значного кода; кнопка входа через
 Telegram внизу сохранена без изменений.
 - Прошёлся по всему репозиторию на предмет старых упоминаний
 passwordless-входа ("без пароля", "одноразовая ссылка", magic-link) —
 поправил копирайтинг на 20 страниц сайта (включая
 `landing/account.html`, `landing/privacy.html` — там формулировка была
 фактически неверной: обещала, что пароли не создаются и не хранятся),
 доки `AGENTS/architecture/api.md`, `AGENTS/architecture/database.md`,
 `ABOUT_PROJECT.md`, `CLAUDE.md`, комментарии в коде (`bot/config.py`,
 `bot/utils/mailer.py`, `bot/models/web_session.py`, `.env.example`).
 - По ходу нашёл и починил битый импорт `bot.models.magic_link` в
 `alembic/env.py`, оставшийся после удаления модели (алембик бы упал
 на любой команде) — заменил на недостающий `bot.models.email_code`
 (в `alembic/env.py` его не было вообще, только в `database.py`).
 - Проверено: `python3 -m py_compile` по всем изменённым `.py`,
 `node --check` по инлайновым `<script>` во всех тронутых `.html`,
 плюс отдельный async-интеграционный тест на SQLite (не закоммичен,
 разовая проверка) для register → verify-code → login.
- **Затронутые файлы/папки:** `bot/models/user.py`,
 `bot/models/email_code.py` (новый), `bot/models/magic_link.py`
 (удалён), `bot/models/web_session.py`, `bot/utils/webauth.py`,
 `bot/utils/mailer.py`, `bot/utils/database.py`, `bot/config.py`,
 `bot/api.py`, `bot/requirements.txt`, `alembic/env.py`,
 `alembic/versions/0008_password_auth.py` (новый),
 `alembic/versions/0009_drop_magic_link_tokens.py` (новый),
 `landing/login.html`, `landing/account.html`, `landing/privacy.html` +
 18 страниц с общей формулировкой в модалке "С чего начать",
 `.env.example`, `AGENTS/architecture/api.md`,
 `AGENTS/architecture/database.md`, `AGENTS/decisions/ADR.md`,
 `ABOUT_PROJECT.md`, `CLAUDE.md`
- **Важно для следующего агента:**
 - На проде нужно применить обе новые миграции
 (`alembic upgrade head`) — без этого `password_hash`/`email_verified`
 и таблица `email_verification_codes` не появятся, регистрация будет
 падать. Автодеплой (`infra/auto-deploy.sh`) сам код обновит, но
 миграции он не запускает — это отдельная ручная команда на сервере.
 - Пока `SMTP_HOST` не задан в `.env` на проде, код подтверждения
 только пишется в лог бота (см. `bot/utils/mailer.py`) — письма
 реально уходить не будут. Это осознанный fallback для тестирования,
 а не баг, но для реального "прям полностью рабочее" на проде нужен
 настоящий SMTP (хост/порт/логин/пароль/from) в `.env`.
 - Пользователь прислал изображение золотого треугольного лого со
 звездой и попросил сделать его фавиконом сайта — **не сделано
 намеренно**: лого визуально почти идентично товарному знаку
 Anthropic, есть риск чужих прав. Дождаться подтверждения
 происхождения/прав на изображение от пользователя, не делать это
 молча.
 - Работа этой сессии на момент записи ещё не закоммичена и не
 запушена — коммит/пуш в `original` ожидается сразу после этой записи.

### 2026-08-30 — Фавикон (звезда, не лого пользователя) + настоящий вход через Telegram (OIDC)

- **Роль:** frontend + backend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - Пользователь дважды настаивал использовать как фавикон присланное
 им золотое треугольное лого — отказался оба раза (см. ADR-008):
 оно визуально совпадает с товарным знаком Anthropic, риск остаётся
 риском независимо от того, что просит владелец продукта. Вместо
 этого сделал оригинальный фавикон — пятиконечная золотая звезда на
 тёмном скруглённом квадрате, `landing/favicon.svg` + растровые
 фолбэки (`favicon.ico`, `favicon.png`, `apple-touch-icon.png`),
 сгенерированные из SVG через headless Chromium (Playwright, т.к. в
 песочнице не было `rsvg-convert`/`cairosvg`). Раньше фавиконом на
 всех 22 страницах сайта/админки был `logo.png` (полноразмерный
 "STAR VPN" wordmark) — на 16px он был практически нечитаем; заменил
 везде на новый набор, плюс роуты `/favicon.svg`, `/favicon.ico`,
 `/favicon.png`, `/apple-touch-icon.png` в `bot/api.py`.
 - Реализовал реальный вход через Telegram на сайте — до этого кнопка
 "Войти через Telegram" на `/login` вела просто на `t.me/starisvpnbot`
 (открывала бота, не логинила на сайте). Пользователь прислал скрин
 настроек Telegram Login Widget (Client ID/Client Secret/Redirect
 URIs/Trusted Origins) и реальный client secret в чат — секрет ушёл
 только в `.env`/конфиг, нигде не закоммичен. Это не classic
 script-embed widget и не Mini App — полноценный OIDC/OAuth2
 authorization-code flow с PKCE через `oauth.telegram.org` (см.
 ADR-007 для разбора трёх похожих механизмов Telegram-логина и почему
 выбран именно этот).
 - Перед реализацией свежий механизм (Client ID/Secret/Redirect URIs —
 непохоже на классический Login Widget) проверил через WebSearch/
 WebFetch по `core.telegram.org/bots/telegram-login`, а не угадывал
 по памяти — получил точные эндпоинты (`oauth.telegram.org/auth`,
 `/token`, JWKS на `/.well-known/jwks.json`) и структуру id_token.
 - Новое: `bot/utils/telegram_oauth.py` (PKCE, authorize URL, обмен
 code→token, проверка подписи id_token через `PyJWKClient`),
 `webauth.get_or_create_user_by_telegram_id` (аналог `cmd_start` из
 `bot/handlers/start.py`, но для реального telegram_id с сайта),
 `GET /api/telegram-oauth/start` и `GET /api/telegram-oauth/callback`
 в `bot/api.py`. `state`+`code_verifier` живут в httponly cookie
 10 минут (`tg_oauth_pkce`) — отдельной таблицы под это не заводил,
 это одноразовый handshake браузера. Добавлены `TELEGRAM_OAUTH_CLIENT_ID`/
 `_SECRET` в `bot/config.py`/`.env.example`, зависимости `PyJWT`,
 `cryptography` в `bot/requirements.txt`.
 - `landing/login.html`: обе Telegram-кнопки (`.nav-tg-btn`, `.tg-btn`)
 теперь ведут на `/api/telegram-oauth/start` вместо диплинка на бота;
 добавлена обработка `?tg_error=1` (редирект при неудачном логине).
 - Протестировано локально в изолированном venv (в основной песочнице
 стоит несовместимый системный `cryptography`/`PyJWT` от Debian):
 PKCE/authorize URL, проверка подписи id_token на самоподписанном
 RSA-ключе с подменённым JWKS-клиентом (в т.ч. отказ по неверной
 audience и по истёкшему `exp`), и `get_or_create_user_by_telegram_id`
 на SQLite (создание + обновление username/full_name при повторном
 входе).
- **Затронутые файлы/папки:** `landing/favicon.svg` (новый),
 `landing/favicon.ico`/`favicon.png`/`apple-touch-icon.png` (новые,
 бинарные), все 22 HTML-страницы `landing/`+`admin/` (замена `<link
 rel="icon">`), `bot/api.py`, `bot/config.py`,
 `bot/utils/telegram_oauth.py` (новый), `bot/utils/webauth.py`,
 `bot/requirements.txt`, `.env.example`, `AGENTS/architecture/api.md`,
 `AGENTS/decisions/ADR.md`, `CLAUDE.md`
- **Важно для следующего агента:**
 - Кнопка "Войти через Telegram" не заработает на проде, пока в
 `.env` не заданы `TELEGRAM_OAUTH_CLIENT_ID`/`TELEGRAM_OAUTH_CLIENT_SECRET`
 и пока в BotFather (бот → Login Widget) не зарегистрированы Redirect
 URI `https://starvpnservice.ru/api/telegram-oauth/callback` и Trusted
 Origin `https://starvpnservice.ru` — без этого `/api/telegram-oauth/start`
 отдаёт 503 (нет ключей) либо Telegram отклонит редирект (нет URI).
 - Presented client secret в этой сессии был вставлен пользователем
 открытым текстом в чат — если это где-то залогировано за пределами
 самой сессии, стоит по возможности перевыпустить (Revoke → новый
 secret в BotFather) и обновить `.env` на сервере.
 - Не путай новый OIDC-логин с уже существующим Mini App `initData` —
 это два независимых пути получить `telegram_id`, оба сходятся в
 `create_web_session`/`_resolve_tg_id`, но триггерятся по-разному
 (редирект браузера vs. открытие внутри Telegram).

### 2026-08-30 — Оплата только ЮMoney + мобильное меню + рекламный баннер + аккаунты админки с рангами

- **Роль:** backend + frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - Оплата: только ЮMoney активна сейчас. Добавил тумблер `stars` в
 `bot/utils/settings_store.py` (раньше Stars были жёстко "всегда
 включены"), дефолты `card=False`/`stars=False`/`crypto=False`
 (было так)/`yoomoney=True`. Основной флоу "⚡️ Подключить VPN" в
 боте при выключенных Stars теперь ведёт на тот же выбор способа
 оплаты, что и "Продлить подписку", а не на Stars-инвойс напрямую.
 На `get-vpn.html` недоступные способы показываются серыми
 "Временно недоступно", а не скрываются — сознательно другой
 выбор, чем на `/account` (там как было — просто не выводятся,
 не трогал, чтобы не рисковать регрессом). См. ADR-010.
 - Мобильное меню на сайте: раньше на ширинах ≤768px пропадали и
 ссылки навигации, и кнопка "Войти" — оставалась только жёлтая
 CTA-кнопка, без способа попасть на Тарифы/Wiki с телефона. Добавил
 гамбургер (☰), раскрывающий список ссылок, и вернул кнопку входа —
 теперь на мобильном 3 полоски + 2 кнопки, как и просили. Правил
 16 файлов сразу (везде идентичный кусок CSS/HTML, нашёл это заранее
 через grep, а не полез в каждый файл руками). По ходу поймал
 реальный баг в собственной первой версии фикса — гамбургер
 продавливал вторую кнопку за пределы экрана на мобильном (проверил
 Playwright-скриншотами на нескольких ширинах, а не поверил на
 слово) — донастроил отступы/размеры и добавил `flex-wrap` как
 подстраховку на совсем узких экранах (320px).
 - Рекламный баннер: раньше верхняя полоса на главной была жёстко
 зашита ("Telegram не открывается?..."). Теперь `AdBanner` (таблица,
 синглтон id=1) редактируется из нового раздела "Реклама" в
 админке (текст, необязательная ссылка, вкл/выкл), отдаётся публично
 через `GET /api/ad-banner`, `landing/index.html` подтягивает и
 показывает баннер только если он включён и есть текст (иначе полоса
 просто не рендерится, без пустого места).
 - Документация покупки: `AGENTS/architecture/purchase-flow.md` —
 три параллельных пути (бот / сайт с аккаунтом / сайт-гость
 `get-vpn.html`). Явно отметил, что решение "требовать ли регистрацию
 всегда при покупке с сайта" НЕ принято этой задачей — гостевой
 чекаут как работал без аккаунта, так и работает; пользователь просил
 "нажал купить без ТГ → регистрация → оплата" — это уже и есть
 существующий путь через `/account`, отдельно от `get-vpn.html`.
 - Аккаунты админки: раньше был один общий `ADMIN_WEB_KEY` на всех.
 Теперь `AdminAccount` (username/password_hash/rank) + `AdminSession`
 (bearer-токен на аккаунт). Два ранга: `admin` (есть личный
 `invite_key`, может приглашать) и `worker` (не может). Самый первый
 `admin`-аккаунт создаётся мастер-ключом — тем же `ADMIN_WEB_KEY`, что
 был раньше. `/admin` теперь показывает вкладки "Войти"/"Первый вход"
 вместо одного поля ключа; новый раздел "Профиль" показывает ранг и
 личный invite_key. Полный разбор компромиссов (почему сессии остались
 bearer-токеном в памяти процесса, а не JWT/cookie) — ADR-009.
 - Протестировано (изолированный venv, тот же приём, что и раньше в
 этой сессии — системный `cryptography` в песочнице конфликтует):
 полный цикл `register_admin`/`login_admin`/`check_session`/
 `logout_admin` на SQLite (bootstrap по мастер-ключу → worker по
 личному инвайту → неверный ключ/пароль отклонены → истёкший/
 разлогиненный токен отклонён), плюс `settings_store`/`AdBanner` на
 SQLite. `py_compile` по всему `bot/`+`alembic/`, `node --check` по
 всем 22 HTML со скриптами. Мобильное меню проверено скриншотами
 Playwright на 320/375/390/430px.
- **Затронутые файлы/папки:** `bot/utils/settings_store.py`,
 `bot/handlers/payment.py`, `bot/handlers/profile.py`,
 `bot/models/admin_account.py` (новый), `bot/models/admin_session.py`
 (новый), `bot/models/ad_banner.py` (новый), `bot/utils/admin_auth.py`
 (новый), `bot/api.py`, `bot/main.py`, `alembic/env.py`,
 `alembic/versions/0010_admin_accounts_and_ad_banner.py` (новый),
 `admin/index.html`, `landing/index.html`, `landing/get-vpn.html` +
 15 других страниц (мобильное меню), `AGENTS/architecture/api.md`,
 `AGENTS/architecture/database.md`, `AGENTS/architecture/purchase-flow.md`
 (новый), `AGENTS/decisions/ADR.md`, `CLAUDE.md`
- **Важно для следующего агента:**
 - На проде нужна ещё одна миграция: `alembic upgrade head` до
 `0010_admin_accounts_and_ad_banner`.
 - Первый вход в обновлённую админку — через вкладку "Первый вход",
 ключ = текущий `ADMIN_WEB_KEY` из `.env`. После этого у владельца
 появится СВОЙ личный `invite_key` (виден в разделе "Профиль") —
 именно им приглашать следующих людей, не мастер-ключом каждый раз
 (хотя мастер-ключ и дальше будет работать для создания ещё одного
 `admin`-аккаунта, если понадобится).
 - Не трогал `bot/handlers/gift.py` — там выбор "⭐ Stars или 💎
 Крипта" для подарков всё ещё не смотрит на тумблеры провайдеров.
 Пользователь не просил про подарки явно — сознательно не расширял
 объём задачи, но это стоит поправить отдельно, если подарки через
 отключённые способы кто-то попробует оформить.
 - `/account` (личный кабинет, уже вошедшие пользователи) при
 выключенном способе оплаты просто не показывает его в списке — не
 привёл к единому виду с "Временно недоступно" на `get-vpn.html`,
 сознательно, чтобы не трогать более сложный/используемый экран без
 явного запроса.

### 2026-08-30 — Мобильное меню: кнопки тоже внутри гамбургера, не в шапке

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - Пользователь уточнил дизайн мобильного меню (после моего первого
 захода в этот же день, где кнопки остались в шапке рядом с
 гамбургером и пришлось ужимать шрифты/паддинги, чтобы не вылезали
 за экран): в шапке должно быть только "STAR VPN" + гамбургер,
 обе кнопки ("Войти в кабинет", "2 дня бесплатно") переезжают
 ВНУТРЬ выпадающего меню вместе со ссылками, а не остаются снаружи.
 - Обернул `.nav-links` и блок кнопок в общий `<div class="nav-menu"
 id="navMenu">` на всех 16 страницах (у index.html кнопки раньше
 были в отдельном `<div style="display:flex...">`, у остальных 15 —
 голыми `<a>` сразу после `.nav-links` — регэксп учёл оба варианта
 и провалидировал, что совпадение нашлось ровно один раз на файл
 перед записью). На десктопе `.nav-menu{display:contents}` — ничего
 не меняется визуально, обёртка не участвует в layout. На мобильном
 `.nav-menu` — сам дропдаун (роль, которую раньше играл `.nav-links`
 напрямую), гамбургер переключает `#navMenu`, а не `.nav-links`.
 - Заодно убрал прошлые "тесные" мобильные правки (ужатые шрифты
 кнопок, уменьшенный gap шапки, `flex-wrap` подстраховку) — они были
 нужны только пока кнопки жили в шапке; теперь кнопки полноразмерные
 внутри дропдауна, тесноты больше нет вообще.
 - Проверено скриншотами Playwright (закрытое/открытое состояние на
 375px) и `node --check` по всем 21 файлу `landing/`.
 - Отдельно проверил на живом домене жалобу "не вижу 404-страницу" —
 код существует и локально работает верно (`TestClient` вернул
 правильный HTML), но `curl` на `starvpnservice.ru/<случайный-путь>`
 всё ещё возвращает голый `{"detail":"Not Found"}` — значит на
 сервере не пересобран контейнер с этим кодом (тот же класс проблемы,
 что была с `/api/account/register` раньше в этой сессии). Не чинил
 сам (нет доступа к серверу) — дал пользователю диагностические
 команды.
- **Затронутые файлы/папки:** `landing/index.html`,
 `landing/tariffs.html`, `landing/terms.html`, `landing/privacy.html`,
 13 файлов `landing/wiki/*.html`
- **Важно для следующего агента:** если пользователь опять пришлёт
 скрин с обрезанной/наползающей кнопкой на мобильном — сначала
 проверь, не пытается ли новая правка вернуть кнопки в шапку рядом с
 гамбургером; текущий сознательный дизайн — кнопки только внутри
 дропдауна, в шапке из интерактивного только гамбургер.

### 2026-08-30 — Иконка для рекламного баннера: выбор из 10 вариантов

- **Роль:** frontend + backend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - `AdBanner.icon` (новая колонка, миграция `0011_ad_banner_icon`,
 дефолт `sparkle`) — ключ из фиксированного набора
 (`AD_BANNER_ICONS` в `bot/api.py`: sparkle/star/fire/gift/percent/
 bell/rocket/heart/zap/clock), не произвольная строка/URL — `POST
 /web/ad-banner` отклоняет неизвестный ключ 400-й.
 - В админке (раздел "Реклама") — сетка 5×2 иконок под полями текста,
 клик подсвечивает выбранную, отправляется вместе с сохранением.
 Тот же `BANNER_ICONS`-объект (ключ → SVG path) продублирован в
 `landing/index.html` и `admin/index.html` — так и раньше делалось на
 этом статичном сайте без шаблонизатора (ADR-003), здесь тот же
 принцип: два places, а не общий JS-модуль.
 - По ходу перерисовал две иконки, которые не читались с первой
 попытки (gift выглядел как стол/урна, rocket — как свеча) —
 проверил скриншотом Playwright перед тем, как показывать
 пользователю, а не поверил на глаз своему SVG-path вручную.
- **Затронутые файлы/папки:** `bot/models/ad_banner.py`,
 `alembic/versions/0011_ad_banner_icon.py` (новый), `bot/api.py`,
 `admin/index.html`, `landing/index.html`
- **Важно для следующего агента:** на проде нужна ещё одна миграция
 (`alembic upgrade head` до `0011_ad_banner_icon`) в дополнение к уже
 накопившимся с этой сессии.

### 2026-08-30 — Мобильная версия админ-панели: боковое меню снизу

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - `admin/index.html` раньше вообще не имел мобильной адаптации на
 уровне разметки: `#app{grid-template-columns:248px 1fr}` без единого
 media query — на телефоне боковое меню съедало 60%+ ширины экрана
 навсегда, контенту оставались крохи. Часть карточных сеток
 (`.dash-grid`, `.support-grid`, `.editor-grid`, `.settings-grid`) уже
 схлопывались в 1 колонку при ≤820-900px — это было сделано раньше и
 не трогал; не было адаптации только у каркаса приложения (сайдбар/
 топбар) и у одного inline-style грида в "Профиле".
 - Добавил `<nav class="bottom-nav" id="bottomNav">` — при ≤860px
 `aside` скрывается, `#app` схлопывается в одну колонку, снизу
 фиксированная горизонтально-скроллящаяся полоса со ВСЕМИ разделами
 (не только "избранными" — сознательно, чтобы ничего не терялось и
 можно было и смотреть, и редактировать любой раздел, как просил
 пользователь). Полоса рендерится в той же `renderSidebar()`, просто
 сплющивая `NAV_GROUPS` в один список — не заводил отдельную функцию
 ради дублирования одного и того же источника данных.
 - `<div style="...grid-template-columns:minmax(0,1fr) 280px...">` в
 Профиле переведён на класс `.profile-grid`, чтобы media query вообще
 могла его перебить (inline-стиль иначе бьёт любой класс без
 `!important` — использовал `!important` в самом media query, раз уж
 стиль всё равно остаётся inline).
 - `.content` получил `padding-bottom` под высоту нижней полосы,
 topbar на мобильном строится в несколько строк (поиск/статус переносятся `order`).
 - Проверено Playwright на 390px по 4 разным экранам (Дашборд,
 Аккаунты-таблица, Настройки, Профиль) — на каждом
 `scrollWidth > clientWidth` === false (нет горизонтального
 переполнения), клик по нижней полосе переключает раздел и подсвечивает
 активный пункт. По ходу поймал, что мои первые тестовые JSON-моки не
 совпадали с реальными полями (`data.users`, не `data.items`;
 `revenue_chart`/`users_chart`, не `chart`) — это не баг кода, поправил
 моки и переснял скриншоты, а не оставил ложную тревогу висеть.
- **Затронутые файлы/папки:** `admin/index.html`
- **Важно для следующего агента:** нижняя полоса показывает ВСЕ
 разделы одним скроллящимся рядом — если добавляешь новый раздел в
 `NAV_GROUPS`, он появится в обоих местах (сайдбар и нижняя полоса)
 автоматически, ничего вручную дублировать не нужно.

### 2026-08-30 — Нижняя навигация: 3 основных пункта + "Меню" с bottom sheet

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - Пользователь попросил переделать только что сделанную нижнюю
 навигацию — не один горизонтально скроллящийся ряд из всех
 разделов, а "3 основных + 4-я кнопка с тремя полосками, при нажатии
 выезжает остальное". Заменил: `BOTTOM_NAV_PRIMARY = ['dash', 'users',
 'payments']` — три фиксированных пункта видны всегда (выбор основан
 на предположении "обзор/люди/деньги" самые частые — не запрашивал
 явно у пользователя, какие именно 3, это предположение можно
 поправить); 4-я кнопка "Меню" (иконка ☰) открывает `.bottom-sheet` —
 выезжающую снизу панель с ПОЛНЫМ списком разделов по группам (та же
 разметка, что в десктопном сайдбаре, просто переиспользована для
 sheet). Оверлей `.bottom-sheet-overlay` затемняет фон и закрывает
 sheet по тапу мимо.
 - "Меню" подсвечивается активным, если текущий раздел не входит в
 `BOTTOM_NAV_PRIMARY` (то есть открыт через sheet) — так пользователь
 видит, где он находится, даже когда сам sheet уже закрыт.
 - Источник данных общий (`NAV_GROUPS`) — три места (десктопный
 сайдбар, 3 нижние кнопки, sheet) не дублируют разметку вручную,
 только фильтруют/используют один и тот же список.
 - Проверено Playwright на 390px: клик по "Меню" открывает панель,
 клик по пункту в панели переключает раздел и закрывает панель.
- **Затронутые файлы/папки:** `admin/index.html`
- **Важно для следующего агента:** если пользователь захочет другие 3
 "основных" пункта вместо Дашборд/Аккаунты/Платежи — это одна строка
 (`BOTTOM_NAV_PRIMARY`), не структурная правка.

### 2026-08-30 — Страницы результата оплаты: фирменный дизайн вместо голого HTML

- **Роль:** frontend + backend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - `/card/success` и `/card/fail` (единый SuccessURL/FailURL для
 Robokassa и ЮMoney, и для гостевого чекаута, и для Telegram-платежей)
 раньше были HTML-строками прямо в `bot/api.py` — простой системный
 шрифт, эмодзи (✅/❌) вместо иконок, без нав-бара/звёздного
 фона/футера, никак не похоже на остальной сайт. Пользователь попросил
 показать — увидев, попросил переделать в фирменном стиле, побольше
 контента, без смайликов.
 - Вынес обе страницы в реальные файлы — `landing/card-success.html`,
 `landing/card-fail.html` — с тем же каркасом, что и на `404.html`
 (нав, `#bg-canvas` звёздный фон на 340 звёзд, футер), а не оставил
 их HTML-строками в Python. `bot/api.py`: `/card/success`/`/card/fail`
 теперь просто `_serve_html(_LANDING_DIR / "card-success.html")` —
 тот же паттерн, что у всех остальных статичных страниц (ADR-003):
 правки контента этих страниц больше не требуют трогать Python-код.
 - Иконки успеха/провала — обычные inline SVG (круг+галочка,
 круг+крестик), а не эмодзи, в цвет `--green`/`--red`, уже
 использовавшийся в админке.
 - Контент стал заметно полнее: на успехе через Telegram — блок
 "Что дальше" с тремя пронумерованными шагами и бейджи Zero
 Logs/до 3 устройств; на успехе гостя (без Telegram) — тот же QR/ссылка,
 что и раньше, плюс новый блок с прямыми ссылками на гайды подключения
 по устройствам (`/wiki/install-ios` и т.д.) — раньше просто отдавали
 голую ссылку и бросали пользователя; на провале — блок "Частые
 причины" (банк отклонил, истекло время, не хватило средств, отменено
 вручную) вместо голого "попробуй ещё раз".
 - JS-логика (poll `/api/guest/order/{id}` по localStorage
 `star_vpn_guest_order`, разные состояния для Telegram- и
 гостевого-возврата) перенесена без изменений в поведении — только
 переоформлена в разметку нового дизайна.
 - Проверено: `python3 -m py_compile bot/api.py`, `node --check` на
 обоих новых файлах, полный прогон через `TestClient` (реальные HTTP
 200 с новым HTML) и 3 скриншота Playwright (успех через бот, успех
 гостя с моком `/api/guest/order/{id}`, провал) — не поверил на слово,
 что вёрстка не разъехалась.
- **Затронутые файлы/папки:** `landing/card-success.html` (новый),
 `landing/card-fail.html` (новый), `bot/api.py`
- **Важно для следующего агента:** если снова понадобится трогать
 текст/дизайн этих страниц — редактируй файлы в `landing/` напрямую,
 не ищи HTML-строку в `bot/api.py`, её там больше нет.

### 2026-08-30 — Кастомный экран загрузки Mini App

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - Пользователь третий раз за сессию прислал золотое треугольное
 "A"-лого и попросил сделать из него/со звёздами кастомный экран
 загрузки — снова отказался использовать именно это изображение
 (см. ADR-008: визуально совпадает с товарным знаком Anthropic,
 контекст использования тут не меняет риск). Сделал экран загрузки на
 основе уже существующего фирменного стиля Mini App (`gold`/`ink`/
 `app.bg` из `tailwind.config.js`, шрифт Space Grotesk) — свой
 4-лучевой sparkle-значок (не треугольник) + "STAR VPN" + текст
 "Загрузка" с пульсацией.
 - `webapp/src/components/Starfield.jsx` — уже существовавший
 canvas-компонент мерцающих звёзд — получил необязательный проп
 `shootingStars` (по умолчанию `false`, не трогает существующее
 поведение фона в остальном приложении): при включении раз в
 1.8–4.4 сек по канвасу пролетает "падающая звезда" (линия с
 градиентом от яркой головы к прозрачному хвосту), как на
 референс-картинке пользователя.
 - `webapp/src/App.jsx`: экран `if (loading || !subscription)` (уже
 существовавшее место, где показывался просто текст "Загрузка...")
 переоформлен в полноценный сплэш — sparkle-иконка + wordmark + текст
 загрузки, фон — `<Starfield shootingStars />`.
 - **Важно:** `webapp/app.html` — собранный артефакт (ADR-003,
 исключение из "статичный HTML без сборки" — Mini App это React).
 Правка одного `src/App.jsx` без пересборки не подхватилась бы на
 проде. Установил зависимости (`npm install` — `node_modules` до
 этого не было) и запустил `npm run build:app`
 (`vite build && cp dist/index.html app.html`) — так это обычно и
 обновляется по докстрингу в ADR-003. `npm run lint` в этом
 окружении не работает вообще (ESLint 9 нужен `eslint.config.js`,
 которого в репо нет — предсуществующая проблема окружения, не
 связанная с этой правкой, не чинил, не входило в задачу).
 - Полноценно проверить экран в самом Mini App через Playwright не
 получилось — `App.jsx` показывает отдельный экран онбординга
 ("Полная анонимность" и т.д.) перед проверкой `loading`, и это
 условие не зависит от ответа API, так что мок сети не помог дойти
 до сплэша через реальный `app.html`. Вместо репорта "не смог
 проверить" — воссоздал ровно ту же canvas-анимацию (звёзды +
 падающие звёзды) один в один в отдельном HTML для скриншота и
 показал пользователю визуально идентичный результат.
- **Затронутые файлы/папки:** `webapp/src/components/Starfield.jsx`,
 `webapp/src/App.jsx`, `webapp/app.html` (пересобран)
- **Важно для следующего агента:** любая правка `webapp/src/**`
 требует `cd webapp && npm run build:app` перед коммитом — иначе
 `app.html`, который реально раздаётся ботом, останется старым.

### 2026-08-30 — Экран загрузки на самом сайте (не Mini App)

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - Пользователь уточнил, что предыдущий сплэш (для Mini App) — не то,
 что он просил, ему был нужен экран загрузки на самом сайте
 (`landing/`). Добавил в `landing/index.html` (главная страница) —
 `#site-splash`: полноэкранный оверлей поверх страницы, с отдельным
 канвасом (свой набор звёзд + периодические "падающие звёзды" каждые
 0.9-1.8с — тот же приём, что и в Mini App splash, но независимая
 копия на чистом JS, не завязана на React), sparkle-иконка + "STAR
 VPN" + пульсирующая надпись "Загрузка".
 - Логика скрытия: слушает `window.onload`, ждёт ещё 500мс для
 плавности, затем плавно скрывает (`opacity` transition .5s) и удаляет
 элемент из DOM; отдельный таймаут на 3.5с — страховка на случай
 медленной загрузки, чтобы сплэш не завис навсегда, если событие
 `load` почему-то не сработает.
 - **Сознательно только на главной** (`index.html`), не на всех
 страницах сайта — сплэш при каждом переходе между внутренними
 страницами (Тарифы → Wiki → и т.д.) было бы навязчиво; на входе на
 сайт (главная) это уместнее. Если пользователь захочет и на других
 страницах — паттерн (разметка + CSS + IIFE-скрипт) можно скопировать
 один в один, я его не абстрагировал в общий файл (сайт без
 шаблонизатора, ADR-003).
 - Проверено Playwright: сплэш виден сразу после загрузки (с падающей
 звездой), плавно исчезает и открывает реальную страницу примерно
 через 1.5-1.7с; отдельно проверил, что `scrollWidth > clientWidth`
 после появления сплэша — это тот же самый пре-существующий
 `.marquee-track` (бегущая строка технологий, специально шире
 экрана), а не что-то, что сломал сплэш.
- **Затронутые файлы/папки:** `landing/index.html`
- **Важно для следующего агента:** если попросят добавить этот же
 сплэш на другие страницы — копируй три куска (HTML `#site-splash`
 после `<body>`, CSS-блок у `#bg-canvas`, IIFE-скрипт "Экран загрузки"
 в начале `<script>`) без изменений, они самодостаточны и не зависят
 от остального кода страницы.

### 2026-08-30 — Экран загрузки: апгрейд по референсу пользователя

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** Первая версия сплэша (см. запись выше в тот же день)
 была слишком минималистичной — пользователь прислал референс
 (крупный светящийся логотип, туманности, прогресс-бар с процентами)
 и попросил "что-то крутое". Доработал `#site-splash` в
 `landing/index.html`:
 - Крупный (`clamp(52px,11vw,110px)`) двухстрочный "STAR"/"VPN" с
 золотым градиентом текста (`background-clip:text`) и свечением
 (`filter:drop-shadow`); буква "A" в STAR заменена на инлайновую
 5-конечную звезду, залитую цветом фона — визуально читается как
 вырез/дырка в золотом слове, не отдельная картинка поверх текста.
 Это собственная типографика на CSS/SVG, не позаимствованный логотип.
 - Два размытых радиальных "туманных" пятна по углам
 (`.splash-nebula`) — чисто CSS, без картинок.
 - Тег-лайн "Быстро · Безопасно · Без границ".
 - Прогресс-бар с живым процентом: анимация ease-в-90% через
 `requestAnimationFrame` (имитация загрузки, не привязана к
 реальному прогрессу — сайт статичный, реального прогресса нет),
 довершается до 100% в момент `window.onload`/через тот же 3.5с
 fallback, что и раньше, только после этого начинается fade-out.
 - "Загрузка" получила анимированные точки (0-3, каждые 350мс).
 - Звёзды/падающие звёзды на канвасе — без изменений от предыдущей
 версии.
 - Проверено скриншотами на 1400px и 420px — оба читаются, ничего не
 обрезано, звезда-вместо-A не сливается с фоном ни на каком размере
 (проверено визуально, не только "должно быть").
- **Затронутые файлы/папки:** `landing/index.html`
- **Важно для следующего агента:** если правишь текст в
 `.splash-word` — учитывай, что там ожидается ровно такая структура
 (`<div>ST<span class="a-star">...</span>R</div><div>VPN</div>`) для
 переноса на 2 строки и звезды на месте A; при простом изменении
 текста на другое слово эта конструкция не подойдёт "из коробки".

### 2026-08-30 — Экран загрузки: настоящий логотип вместо CSS-текста

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** Пользователь прислал скриншот из Photoroom (свой
 существующий вордмарк "STAR VPN" с падающими звёздами, фон вырезан
 в редакторе) и попросил поставить именно его на экран загрузки.
 Скриншот был не готовым PNG-ассетом, а фотографией интерфейса
 Photoroom — вокруг логотипа оставались чужеродные элементы (кружки
 превью инструментов, цветной градиентный блик, пунктирные линии
 разметки редактора), а "прозрачность" в самом скриншоте — это
 растровый шахматный узор, а не настоящий alpha-канал. Вытащил
 логотип программно:
 1. Проанализировал изображение построчно (`PIL` + `numpy`), нашёл
 границы шахматного паттерна по яркости/насыщенности пикселей и
 обрезал экранные элементы интерфейса сверху/снизу отдельным
 проходом (они не удалялись только цветовым порогом, т.к. попадали
 в диапазон "непрозрачного").
 2. Пересчитал шахматный узор (светлые ненасыщенные пиксели) в
 настоящий alpha-канал: `is_bg = lightness>195 & saturation<18` →
 alpha=0, с мягкой полупрозрачной каймой на границе для сглаживания
 краёв букв.
 3. Обрезал результат по bounding box непрозрачных пикселей,
 сжал до 700px по ширине (был 1170px — с самим шахматным полотном
 экрана телефона, избыточно для web).
 4. Не поверил на слово, что вырезка чистая — скомпоновал результат
 на сплошном тёмном фоне и посмотрел глазами перед тем, как считать
 готовым; с первого раза в кадр попал кусок интерфейса Photoroom
 (кружки/пунктир) — дообрезал второй итерацией.
 - `landing/logo-star-transparent.png` (новый файл) отдаётся через
 выделенный роут `bot/api.py` (`/logo-star-transparent.png`) — тот
 же паттерн, что у `/logo.png`/`/logo-title.png`, отдельного
 статик-маунта на сайте нет (ADR отсутствует, просто следование
 существующему стилю кода).
 - `landing/index.html`: CSS-сгенерированный вордмарк (двухстрочный
 текст с inline-SVG звездой вместо буквы A из предыдущей версии
 сплэша) заменён на `<img src="/logo-star-transparent.png">` с
 `filter:drop-shadow` для свечения. Остальное (звёзды/падающие
 звёзды на канвасе, тег-лайн, прогресс-бар) — без изменений.
- **Затронутые файлы/папки:** `landing/logo-star-transparent.png`
 (новый), `bot/api.py`, `landing/index.html`
- **Важно для следующего агента:** если пользователь пришлёт ещё один
 "прозрачный" логотип скриншотом из редактора (Photoroom/Remove.bg/
 etc.) — скорее всего это будет тот же случай (шахматный узор +
 обрезки интерфейса, не настоящий PNG с альфа-каналом). Тот же
 пайплайн (найти границы контента → цветовой порог по
 яркости/насыщенности → обрезать UI-мусор отдельно → визуально
 проверить на тёмном фоне) должен сработать снова.

### 2026-08-30 — Чистка "нелогичных моментов" + правки сплэша

- **Роль:** backend + frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - Логотип на сплэше (`landing/logo-star-transparent.png`) был почти
 800px пустого прозрачного холста вокруг самого текста плюс 4
 огрызка декоративных штрихов по углам — из-за этого тег-лайн
 визуально "улетал" далеко вниз от вордмарка. Обрезал PNG по
 bounding box самого текста (нашёл его через `scipy.ndimage.label`
 по alpha-каналу — крупные компоненты это буквы, мелкие — огрызки
 штрихов, убрал мелкие). Заодно переставил "Загрузка…" под
 прогресс-бар вместо над ним по просьбе пользователя.
 - По просьбе "какие есть нелогичные моменты, исправь все" нашёл и
 починил 3 вещи: (1) подарок в боте (`bot/handlers/gift.py`) всегда
 предлагал оплату Stars, даже когда тумблер `stars` выключен
 (крипта уже была правильно завязана на тумблер, Stars — нет) —
 добавил такую же проверку; (2) `_mz_username_for_type()`
 (продублирована в `bot/api.py` и `bot/handlers/devices.py`) при
 отсутствии Telegram-username подставляла `str(telegram_id)` в имя
 Marzban-пользователя — для веб-аккаунтов (email-регистрация) это
 отрицательное число, получалось имя со знаком минус
 (`ios_tg_-183`), что ломает создание устройства именно для этого
 сегмента; заменил на `web{id}` без минуса при отрицательном ID;
 (3) wiki/тарифы/FAQ/оферта на сайте всё ещё рекламировали Stars и
 карту (Robokassa) как рабочие способы оплаты — переписал на факт
 "сейчас работает только ЮMoney, остальное временно выключено".
- **Затронутые файлы/папки:** `landing/logo-star-transparent.png`,
 `landing/index.html`, `bot/handlers/gift.py`, `bot/api.py`,
 `bot/handlers/devices.py`, `landing/wiki/payment.html`,
 `landing/tariffs.html`, `landing/wiki/faq.html`, `landing/wiki/index.html`,
 `landing/get-vpn.html`, `landing/terms.html`
- **Важно для следующего агента:** это НЕ было полным аудитом всего
 проекта — только то, что нашлось за разумное время. Отдельно
 стоит отметить: покупка подарка с сайта (`/api/gift/invoice` в
 `bot/api.py`, вкладка "Подарить" на `/account`) уже давно работает
 через карту/ЮMoney и требует знать @username получателя заранее —
 это отдельный, более функциональный путь, чем Stars/крипта-подарок
 в `gift.py`, я про него не знал на момент первого прохода по теме
 платёжных тумблеров.

### 2026-08-30 — Подарок по ссылке: открытка с анимацией на /gift/{code}

- **Роль:** backend + frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** пользователь попросил "прислать ссылку на подарок,
 чтобы там открывалась анимация, поздравление и т.д." Сайтовый
 подарок (`/account` → "Подарить", `POST /api/gift/invoice`) уже
 работал через карту/ЮMoney, но требовал заранее знать @username
 получателя — сделал вторую опцию доставки, "по ссылке", поверх
 того же механизма, не трогая существующий recipient-флоу:
 - `Payment` (`bot/models/payment.py`) получил `gift_link_code`
 (уникальный, nullable) и `gift_claimed` (bool) —
 `alembic/versions/0012_gift_link_payment.py`. Для ссылочного
 подарка получатель ещё не известен на момент оплаты, поэтому
 `Payment.telegram_id` (он NOT NULL + FK на users) временно = сам
 отправитель, пока подарок не заберут — так не пришлось ослаблять
 схему.
 - `POST /api/gift/invoice`: если `recipient` не передан — это режим
 "по ссылке": пропускает поиск получателя, генерирует
 `gift_link_code`, возвращает `gift_link` в ответе сразу же (ещё до
 оплаты) — сайт показывает ссылку с копированием рядом с кнопкой
 "Оплатить", получателю просто ничего не будет видно на странице,
 пока оплата не пройдёт.
 - **Критично:** `handle_card_webhook`/`handle_yoomoney_webhook`
 раньше сразу выдавали подписку на `payment.telegram_id` при
 успешной оплате — для ссылочного подарка это был бы сам
 отправитель. Добавил в оба ранний выход: если `gift_link_code`
 задан, просто помечаем `paid` и выходим, ничего не выдаём.
 Подписку получает тот, кто заберёт по ссылке.
 - Новые публичные роуты в `bot/api.py`: `GET /gift/{code}` (отдаёт
 `landing/gift-reveal.html`), `GET /api/gift/link/{code}` (статус
 без авторизации — `not_found`/`pending`/`claimed`/`ready` + тариф/
 отправитель/сообщение), `POST /api/gift/link/{code}/claim`
 (требует авторизацию через `_resolve_tg_id` — Mini App initData
 или `star_session` cookie; на 401 фронт должен предложить войти).
 Клейм переиспользует `_grant_subscription` (тот же хелпер, что и
 во всех остальных платёжных вебхуках) и создаёт настоящую
 `GiftNotification` — поэтому уже существующий попап "Вам подарок"
 на `/account` (`showGiftPopup`) отрабатывает и для ссылочных
 подарков бесплатно, ничего в нём не менял.
 - `landing/gift-reveal.html` (новый файл) — открытка в стиле сайта
 (тот же `#bg-canvas` со звёздами, что на `card-success.html`):
 CSS-анимация коробки с крышкой (`.box-stage.open` поворачивает
 `.box-lid`), конфетти при успешном клейме (тот же приём, что
 `renderConfetti()` в `account.html`, отдельная копия — в этом
 проекте нет общего JS-модуля для `landing/*.html`, см. ADR-003).
 Если клейм вернул 401 — не городил отдельный OAuth-редирект с
 `next=`, а просто кладёт код в `localStorage` (`star_vpn_pending_gift`)
 и показывает 2 кнопки входа (`/api/telegram-oauth/start` и
 `/login`); `landing/account.html` при `boot()` проверяет этот же
 ключ и добивает клейм автоматически, как только пользователь
 залогинен.
 - `landing/account.html`, вкладка "Подарить": добавил переключатель
 "Знаю получателя" / "Пришлю ссылкой" (переиспользовал существующие
 классы `.provider-toggle/.provider-btn`). В режиме ссылки форма
 тарифа открывается сразу, без шага "Проверить получателя"; после
 `submitGift()` вместо немедленного редиректа на оплату показывает
 карточку со ссылкой (копировать) + отдельной кнопкой "Оплатить".
 - Проверено Playwright-скриншотами: все состояния `/gift/{code}`
 (ready/pending/claimed/not_found/401-с-предложением-войти) и оба
 режима вкладки "Подарить" на `/account`, без JS-ошибок в консоли.
 Прогнал через `TestClient` с sqlite — `not_found`/401/сама HTML-
 страница отвечают ожидаемо; полный happy-path через реальный
 Marzban не тестировал (в песочнице нет доступа к серверу Marzban,
 `_grant_subscription` там просто зависает на сетевом вызове) — но
 это тот же самый, уже боевой хелпер, что и во всех остальных
 платёжных вебхуках, отдельно его не трогал.
- **Затронутые файлы/папки:** `bot/models/payment.py`,
 `alembic/versions/0012_gift_link_payment.py`, `bot/api.py`,
 `bot/handlers/card_payment.py`, `bot/handlers/yoomoney_payment.py`,
 `landing/gift-reveal.html` (новый), `landing/account.html`
- **Важно для следующего агента:**
 - На сервере после деплоя нужно применить миграцию `0012` (`alembic
 upgrade head`), иначе `gift_link_code`/`gift_claimed` не появятся
 в таблице `payments` и режим "по ссылке" будет падать с ошибкой БД.
 - Подарок по ссылке пока работает только через сайтовый
 `/api/gift/invoice` (карта/ЮMoney). Бот-флоу Stars/крипта
 (`bot/handlers/gift.py`) по-прежнему требует знать получателя
 заранее — сознательно не трогал его в этой задаче, чтобы не
 расширять и без того больной по payload-формату код; если
 попросят "ссылку" и для Stars/крипты — потребуется свой
 `giftlink:`-payload и функция активации, симметричная
 `handle_gift_payment`, плюс проброс кода через оба вебхука
 (Stars success + `handle_crypto_webhook`).
 - Код ссылки генерируется `secrets.token_urlsafe(9)` с обрезкой
 небезопасных для URL символов до ~11 символов — умышленно даже с
 запасом, чтобы не заниматься подбором чужих подарков.

### 2026-08-30 — Оплата: убрали и ЮMoney тоже (теперь все способы выключены)

- **Роль:** backend + frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** пользователь попросил убрать оплату через ЮMoney
 "везде, где только есть" — та же команда "выключить тумблером, не
 удалять код", что и в ADR-010, только теперь применённая к
 последнему оставшемуся активному способу. Итог: card, yoomoney,
 crypto, stars — все `False` по умолчанию
 (`bot/utils/settings_store.py`). Единственный оставшийся способ
 получить VPN — бесплатный 2-дневный пробный период.
 - Раз "все способы выключены разом" оказалось не гипотетическим, а
 реальным состоянием, добавил явную обработку там, где её не было:
 `bot/handlers/profile.py` — новая `pay_choice_text()` показывает
 "Оплата временно недоступна" вместо пустого "Выбери способ
 оплаты" с одной кнопкой "Назад"; переиспользуется в
 `bot/handlers/payment.py` (`show_plans`/`show_plans_inline`).
 `landing/get-vpn.html::loadProviders()` — раньше при выборе тарифа
 кнопка "Оплатить" всё равно активировалась (`selectPlan()` не
 проверял доступность провайдера), нажатие падало с невнятной
 ошибкой; теперь кнопка блокируется явным текстом "Оплата временно
 недоступна", как только выясняется, что доступных провайдеров
 нет. Проверил Playwright-скриншотом.
 - `landing/account.html`: вкладки "Продление"/"Подарить" уже умели
 показывать "Оплата временно недоступна" при пустом списке тарифов
 (не трогал эту логику) — просто убрал из текста ссылку на "или
 продлите через бота", раз в боте оплата тоже выключена.
 - Переписал маркетинговые тексты, которые буквально в прошлой
 задаче того же дня стали говорить "сейчас работает только ЮMoney"
 — теперь это тоже неверно. `wiki/payment.html` (самая большая
 правка — весь раздел "способы оплаты" теперь про то, что оплата
 приостановлена, с прежним содержанием обёрнутым в "как это обычно
 работает"), `wiki/faq.html`, `wiki/index.html`, `tariffs.html`
 (карточки способов оплаты, FAQ, meta description, CTA-полоса),
 `landing/index.html` (2 FAQ-пункта), `landing/get-vpn.html` (hero-
 текст, meta description, 2 FAQ-пункта). `terms.html`/`privacy.html`
 не трогал — там способы описаны как "какие вообще поддерживаются"
 (юридическое перечисление процессоров), а не "что работает сейчас",
 остаются верными независимо от тумблеров.
- **Затронутые файлы/папки:** `bot/utils/settings_store.py`,
 `bot/handlers/profile.py`, `bot/handlers/payment.py`,
 `landing/get-vpn.html`, `landing/account.html`,
 `landing/wiki/payment.html`, `landing/wiki/faq.html`,
 `landing/wiki/index.html`, `landing/tariffs.html`, `landing/index.html`
- **Важно для следующего агента:**
 - На проде тумблер ЮMoney скорее всего уже физически лежит в
 таблице `app_settings` со значением "1" (либо никогда не
 создавался и брался из дефолта Python) — смена дефолта в коде не
 переписывает уже существующую строку в БД. Сказал пользователю
 явно зайти в `/admin` → Настройки и выключить ЮMoney вручную, на
 всякий случай, независимо от того, применится ли новый дефолт.
 - Не проверял Mini App (`webapp/`) — там свой (в основном на
 моках, не на реальных тумблерах) экран оплаты, возможно с той же
 дырой "все способы выключены — кнопка всё равно активна". Не
 трогал, т.к. это не основной работающий канал оплаты в проекте
 (см. предыдущие записи).
 - Если попросят "включи что-нибудь одно" — это просто
 `set_provider_enabled(session, provider, True)` через `/admin` →
 Настройки, deploy не нужен.

### 2026-08-30 — Карта + крипта вместо ЮMoney, покупка на сайте требует регистрацию

- **Роль:** backend + frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** пользователь: (1) убрать бейдж "без бота и регистрации"
 на `get-vpn.html`, раз регистрация обязательна; (2) вместо ЮMoney на
 сайте — карта и криптовалюта; (3) убрать везде фразы "оплата
 криптовалютой убрана/недоступна" (их наплодили предыдущие 2-3 задачи
 этого же дня); (4) страницы вроде `get-vpn.html` сделать в 3 шага:
 тариф → аккаунт (регистрация обязательна) → оплата картой/крипто.
 - `settings_store`: `card=True`, `crypto=True`, `yoomoney=False`,
 `stars=False`.
 - Новые эндпоинты `GET /api/crypto/plans` + `POST /api/invoice/crypto`
 в `bot/api.py`, зеркальные card/yoomoney — с тем же payload-форматом
 `"{plan}:{tg_id}"`, что уже понимает `/crypto/webhook`, так что сам
 вебхук трогать не пришлось.
 - Нашёл и починил реальный баг по пути: CryptoPay-инвойс всегда вёл
 кнопкой "оплачено" в бота (`paid_btn_url=t.me/{bot}`) — для
 веб-only аккаунта (email-регистрация, отрицательный telegram_id) это
 значило, что после оплаты открывается бот под ДРУГИМ (настоящим)
 telegram_id, где подписки не видно — деньги как будто "теряются" на
 невидимом веб-аккаунте. Добавил `paid_btn_name`/`paid_btn_url`
 параметры в `cryptopay.create_invoice()` (дефолт не менял — старые
 вызовы из бота ведут себя как раньше), новый сайтовый эндпоинт
 передаёт `paid_btn_name="callback"` → `{SITE_URL}/account`.
 - `get-vpn.html` — полная переделка в 3 явных шага (см. ADR-013 для
 всех деталей). Главное: шаг 3 (оплата) теперь физически заблокирован
 (`.locked`, `pointer-events:none`), пока `GET /api/me` не подтвердит
 сессию — раньше можно было платить вообще без аккаунта через
 `/api/guest/checkout`, теперь платёж идёт через авторизованные
 `/api/invoice/card`/`/api/invoice/crypto`, привязанные к tg_id из
 cookie/initData. Сознательно выпилил кастомный срок (слайдер
 "свой срок 7–180 дней") — у авторизованных инвойсов нет
 произвольного количества дней, только 3 фикс-тарифа; это отдельная
 задача, если понадобится вернуть.
 - Возврат из логина на `/get-vpn` не делал (не стал трогать
 OIDC-callback/email-login редиректы, они жёстко ведут на
 `/account`) — вместо этого выбранный тариф кладётся в
 `localStorage['star_vpn_pending_purchase']`, и `account.html::boot()`
 сам подхватывает его и переключает на вкладку "Продление" с уже
 выбранным тарифом (`resumePendingPurchase()`). Тот же приём, что и
 для отложенного клейма подарка по ссылке из прошлой задачи.
 - `card-success.html`: переименовал `telegramState()` в
 `accountState()`, добавил проверку `/api/me` — если `telegram_id
 <= 0` (веб-only аккаунт), шаги "что дальше" ведут в личный кабинет,
 а не "вернитесь в Telegram-бота". Это была скрытая проблема для
 ЛЮБОЙ сайтовой оплаты не через бота (включая уже существовавший
 `/account` → "Продление"), просто раньше никто не проверял этот
 путь пристально.
 - Прогнал через Playwright оба состояния `get-vpn.html`
 (неавторизован → шаг 3 заблокирован; авторизован → шаг 3 открыт,
 переключение card/crypto меняет цену ₽/$ корректно) и `account.html`
 → "Продление" с криптой. Без JS-ошибок в консоли.
 - Переписал (в который раз за этот день) тексты "оплата
 криптовалютой убрана/недоступна" на "карта или криптовалюта" —
 `wiki/payment.html`, `wiki/faq.html`, `wiki/index.html`,
 `tariffs.html`, `index.html`, `privacy.html`, `terms.html`.
- **Затронутые файлы/папки:** `bot/api.py`, `bot/utils/cryptopay.py`,
 `bot/utils/settings_store.py`, `landing/get-vpn.html`,
 `landing/account.html`, `landing/card-success.html`,
 `landing/wiki/payment.html`, `landing/wiki/faq.html`,
 `landing/wiki/index.html`, `landing/tariffs.html`, `landing/index.html`,
 `landing/privacy.html`, `landing/terms.html`
- **Важно для следующего агента:**
 - `/api/guest/*` и `GuestOrder` НЕ удалены и всё ещё работают — просто
 `get-vpn.html` их больше не вызывает. Если где-то есть прямая ссылка
 на старый гостевой флоу в обход `get-vpn.html`, она продолжит
 пускать без регистрации — это расхождение, а не баг, но стоит знать.
 - Крипта на сайте есть только для обычной покупки/продления
 (`/account` → "Продление"), НЕ для сайтового подарка (`/account` →
 "Подарить", `/api/gift/invoice` принимает только card/yoomoney).
 Подробности и что нужно доделать, если попросят — см. ADR-013,
 последний пункт "Последствия".
 - Оплата криптой в принципе требует Telegram (ограничение самого
 CryptoPay/@CryptoBot) — даже для email-аккаунта. Это явно написано
 в FAQ, не пытался это обойти.

### 2026-08-30 — Единое меню везде: гамбургер + профиль справа, если вошёл

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** пользователь заметил, что шапка сайта неоднородна:
 14 страниц (`index.html`, `tariffs.html`, `terms.html`, `privacy.html`,
 все `wiki/*.html`) имели гамбургер-меню, но не показывали пилюлю
 профиля вместо "Войти в кабинет" тем, кто уже вошёл — эта логика
 существовала только на `get-vpn.html`. При этом у самого
 `get-vpn.html` не было гамбургера вообще: `.nav-links{display:none}`
 на ширинах ≤768px без единого способа их открыть — ссылки на
 мобильном были полностью недостижимы.
 - Унифицировал везде (17 файлов: 16 + get-vpn.html): CSS-блок
 `.account-btn/.account-pill/.account-avatar/.account-dropdown/
 .dropdown-item/.dropdown-divider` (взят как есть с `get-vpn.html`),
 HTML-разметка пилюли+дропдауна (Подписка/Устройства/Приглашения/
 Профиль/Выйти, все ссылки на `/account`), и JS IIFE, который дёргает
 `GET /api/me` и переключает `#nav-login` ↔ `#account-btn.show`.
 Сделал это скриптом (`python3` find-and-replace по трём точным
 якорям: CSS-правило `.nav-login:hover{...}`, HTML-строка
 `<a class="nav-login" href="/login">...</a>`, и последний `</script>`
 перед `</body>`) — во всех 16 файлах эти три якоря были
 побайтово идентичны, проверил `grep -c` заранее, прежде чем
 запускать замену вслепую.
 - `get-vpn.html` отдельно: добавил `.nav-hamburger`/`.nav-menu` CSS
 (взято с `index.html`) и обернул `.nav-links`+`.nav-actions` в
 `#navMenu` — теперь ведёт себя как остальные страницы.
 - Проверил Playwright: на 4 страницах (`index`, `get-vpn`, `tariffs`,
 `wiki/payment`) — гамбургер открывается без горизонтального
 переполнения самого `#nav`, пилюля профиля появляется только когда
 `/api/me` отвечает 200 и корректно прячет "Войти в кабинет", дропдаун
 профиля открывается и виден внутри мобильного меню. Ловил
 false-positive per общий `document.documentElement.scrollWidth` (тот
 же `.marquee-track`/`.splash-nebula`, что и в прошлый раз в этом
 файле) — не чинил, это не связано с навигацией.
- **Затронутые файлы/папки:** `landing/index.html`, `landing/tariffs.html`,
 `landing/terms.html`, `landing/privacy.html`, `landing/get-vpn.html`,
 `landing/wiki/*.html` (все 10 файлов)
- **Важно для следующего агента:**
 - Сознательно НЕ трогал `login.html`, `support.html`,
 `card-success.html`, `card-fail.html`, `404.html`, `gift-reveal.html`
 — это минималистичные утилитарные страницы (только лого в шапке, без
 `.nav-links` вообще, кроме `login.html` у которого есть nav-links, но
 нет hamburger и своя другая CTA-кнопка вместо login/аккаунт-пилюли).
 Если попросят "и там тоже" — паттерн уже понятен, но это не то же
 самое copy-paste (`login.html` не должен предлагать "Войти" самому
 себе, нужно решить, что показывать вместо этого залогиненным).
 - `account.html` не трогал — у него свой другой, полноценный
 app-шелл (сайдбар + bottom-nav), а не маркетинговая шапка, дизайн
 уже соответствует своей странице.

### 2026-08-30 — "2 дня бесплатно": нашёл, почему выбор Telegram/сайт пропадал навсегда

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** пользователь пожаловался, что кнопка "2 дня бесплатно"
 вместо выбора "Telegram или аккаунт" сразу ведёт на регистрацию.
 Модалка выбора канала (`#start-modal`, уже была реализована на всех
 16 маркетинговых страницах) на самом деле работает правильно ПРИ
 ПЕРВОМ клике — но выбор сохранялся в `localStorage['star_vpn_channel']`
 навсегда и на весь домен: стоило один раз нажать "через сайт" на
 любой странице, и после этого кнопка на ЛЮБОЙ странице сайта
 переставала показывать модалку и сразу вела на `/login` — что
 пользователь и наблюдал ("сразу регистрация"). Убрал персист
 полностью — модалка теперь спрашивает каждый раз (в рамках одной
 загрузки страницы решение не переспрашивается повторно, но при
 следующем визите/клике — снова вопрос).
 - Заодно добавил то, что явно просили: клик по кнопке теперь сначала
 проверяет `GET /api/me` — если уже вошли, модалка вообще не
 показывается, сразу редирект на `/account` (незачем спрашивать
 Telegram/сайт и тем более предлагать регистрацию тому, кто уже
 внутри). Если не вошли — открывается модалка; выбор "Через сайт" в
 ней тоже сначала проверяет `/api/me` и ведёт на `/account` (если
 вдруг уже авторизован) либо на `/login` (регистрация/вход) —
 именно так, как просил пользователь: "если аккаунт и не вошёл —
 тогда регистрация или войти".
 - На `index.html` тот же обработчик общий для кнопки в шапке
 (`nav-start-btn`) и в hero-блоке (`hero-primary-btn`) — убрал заодно
 привязанную к тому же localStorage логику подмены текста кнопок
 ("Начать в Telegram"/"Войти по email" и т.п.), она была нужна только
 для "запоминания" выбора, которое только что убрали; кнопки теперь
 просто ведут себя одинаково на каждой загрузке.
 - Правил 16 файлов скриптом (одинаковый JS-анкор, как и в прошлый
 раз) — `index.html` вручную (более длинный, свой вариант с hero-
 кнопками), остальные 15 — python-скриптом с точным совпадением
 старого блока.
 - Не трогал ДРУГОЙ, отдельный механизм на 4 страницах установки
 (`wiki/install-*.html`) — "Subscription source picker", который тоже
 использует ключ `star_vpn_channel` в localStorage, но для другой
 цели (помнить, откуда у пользователя ключ — с сайта или из бота,
 чтобы показывать нужную подсказку на будущих визитах). Совпадение
 имени ключа случайное, к багу с "2 дня бесплатно" отношения не имеет.
 - Проверил Playwright: свежий клик без localStorage → модалка
 открывается; клик "через сайт" → `/login`; клик при ЗАРАНЕЕ
 выставленном `star_vpn_channel=site` в localStorage → модалка всё
 равно открывается (подтверждает, что баг именно в этом и был);
 клик авторизованным пользователем → сразу `/account`, модалка не
 показывается вообще.
- **Затронутые файлы/папки:** `landing/index.html`, `landing/tariffs.html`,
 `landing/terms.html`, `landing/privacy.html`, `landing/wiki/*.html`
 (все 10 файлов)
- **Важно для следующего агента:** если снова попросят подобное —
 паттерн "проверить /api/me перед действием, не полагаться на
 localStorage для решений, влияющих на маршрутизацию" стоит
 использовать по умолчанию для подобных CTA. localStorage годится
 для UI-подсказок (как в install-*.html), но не для того, что решает,
 куда пользователя вести.

### 2026-08-30 — Пилюля профиля на мобильном: рядом с гамбургером, а не внутри него

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** после прошлой унификации меню пилюля профиля
 (`#account-btn`) на мобильном пряталась внутри раскрывающегося
 гамбургер-меню — пользователь хотел, чтобы она была видна сразу,
 рядом с гамбургером (слева от "трёх палок"), без необходимости его
 открывать.
 - Вынес `#account-btn` из `.nav-actions` (внутри `.nav-menu`,
 которое на мобильном полностью скрыто до открытия) в новый
 контейнер `.nav-right`, физический сосед `.nav-hamburger` —
 оба вне `.nav-menu`, значит не прячутся вместе с раскрывающимся
 меню.
 - На мобильном (`@media max-width:768px`) `margin-left:auto`
 перенесён с `.nav-hamburger` на `.nav-right` — теперь пилюля и
 гамбургер вместе прижимаются к правому краю, с пилюлей слева.
 - На десктопе `.nav-menu` — `display:contents` (её дети, ссылки и
 login/CTA, разворачиваются прямо в `.nav-inner`), поэтому простое
 перемещение `.account-btn` в DOM сломало бы порядок — пилюля стала
 бы рендериться сразу после лого, ДО ссылок навигации. Поймал это
 скриншотом до того как показать пользователю, не понадеялся на
 код "должен же работать". Починил через `.nav-right{order:2}` —
 десктопный порядок остался прежним (пилюля по-прежнему в самом
 конце, после "2 дня бесплатно"), а на мобильном `order` не мешает,
 т.к. `.nav-menu` там `display:none` и не участвует в раскладке,
 пока не открыт.
 - Второй баг, тоже пойманный скриншотом, а не предположением:
 длинный email (`friend@example.com`) в пилюле физически выталкивал
 гамбургер за пределы экрана на 390px-ширине — `.account-pill` был
 `white-space:nowrap` без `max-width`, что было нормально, пока
 пилюля пряталась в полноширинном дропдауне, но не на узкой верхней
 панели. Добавил `#account-email{max-width:90px;overflow:hidden;
 text-overflow:ellipsis}` в мобильный media query — обрезается
 многоточием, гамбургер больше не выталкивается.
 - Правил скриптом все 17 файлов (16 + `get-vpn.html` — у него была
 своя, вручную написанная разметка с другими отступами, точный текстовый
 анкор не совпал, чинил его вручную отдельным `Edit`).
 - Проверил Playwright: на 390px без авторизации — только гамбургер,
 ничего не меняется; с авторизацией — пилюля видна сразу в шапке,
 `getBoundingClientRect()` гамбургера целиком в пределах вьюпорта
 (было `right:401` при вьюпорте 390 — вылезал за край, стало
 `right:366` — умещается); дропдаун открывается и работает на новом
 месте; на десктопе (1200px) пилюля по-прежнему в конце ряда, после
 "2 дня бесплатно", как и было до этой правки.
- **Затронутые файлы/папки:** `landing/index.html`, `landing/tariffs.html`,
 `landing/terms.html`, `landing/privacy.html`, `landing/get-vpn.html`,
 `landing/wiki/*.html` (все 10 файлов)
- **Важно для следующего агента:** при любых будущих правках порядка
 элементов в `.nav-inner` помни, что `.nav-menu{display:contents}`
 на десктопе делает `order` единственным надёжным способом управлять
 визуальным порядком независимо от DOM — простая перестановка тегов
 достаточна только для мобильной раскладки (там `.nav-menu` не
 участвует в layout, пока закрыт), но ломает десктопную, если не
 задать `order` явно.

### 2026-08-30 — Десктоп: сначала аккаунт/вход, кнопка "2 дня бесплатно" — справа

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** на десктопе после предыдущей правки порядок был
 "ссылки меню → кнопка '2 дня бесплатно' → пилюля аккаунта", пользователь
 попросил обратный порядок — сначала аккаунт (или "Войти в кабинет",
 если не вошёл), кнопка справа.
 - `.nav-actions` (обёртка вокруг `#nav-login` и `.nav-cta`) на
 десктопе раньше не была `display:contents`, поэтому вела себя как
 один flex-item с `order:0` — рендерилась перед `.nav-right`
 (`order:2`), где живёт пилюля аккаунта.
 - Добавил `@media(min-width:769px){.nav-actions{display:contents}
 .nav-login{order:2}.nav-cta{order:3}}` — теперь `#nav-login` и
 `.nav-cta` сами становятся flex-items `.nav-inner`, `#nav-login`
 встаёт вровень с `.nav-right` (при равном order побеждает DOM-порядок,
 `.nav-right` идёт первым), а `.nav-cta` — после всех.
 - Мобильную раскладку не трогал: там `.nav-actions{display:flex}`
 задан внутри своего `@media(max-width:768px)` и, находясь ниже по
 каскаду при той же специфичности, продолжает побеждать на узких
 экранах.
 - Правил скриптом все 16 файлов с `.nav-cta` (все, кроме
 `get-vpn.html` — там нет этой кнопки, и `support.html` — у него до сих
 пор нет унифицированного меню, см. предыдущую заметку).
 - Проверил Playwright на `index.html`/`tariffs.html` (1280px и 390px,
 с авторизацией и без): десктоп без входа — `Тарифы → "Войти в
 кабинет" → "2 дня бесплатно"`; десктоп с входом — `Тарифы → пилюля
 аккаунта → "2 дня бесплатно"`; мобильная раскладка (шапка и
 гамбургер) не изменилась в обоих состояниях.
- **Затронутые файлы/папки:** `landing/index.html`, `landing/tariffs.html`,
 `landing/terms.html`, `landing/privacy.html`, `landing/wiki/*.html`
 (все 12 файлов, кроме `index.html`/`faq.html`, которые уже перечислены).
- **Важно для следующего агента:** `support.html` по-прежнему не имеет
 унифицированной шапки (ни гамбургера, ни пилюли аккаунта, ни этого
 порядка) — не трогал, пользователь не просил, но это единственная
 страница с `.nav-cta`, оставшаяся вне унификации.

### 2026-08-30 — ЮMoney: "навсегда", не "временно"; убрал ложь про недоступность Stars (ADR-014)

- **Роль:** backend + frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** пользователь указал на две проблемы с копирайтом ADR-013
 (принятым несколькими часами ранее): (1) отключение ЮMoney описывалось
 как временное ("сейчас временно отключены", "могут вернуться"), хотя
 это окончательное бизнес-решение; (2) сайт утверждал, что Telegram
 Stars недоступны — это неправда, Stars реально работает.
 - `settings_store._DEFAULT_ENABLED['stars']` вернул в `True` (было
 `False` с ADR-013) — дефолт для свежего деплоя теперь соответствует
 реальности, а не случайно попал в общий тумблер с ЮMoney.
 - Прошёлся по всем формулировкам "ЮMoney и Telegram Stars сейчас
 временно отключены" (`index.html`, `tariffs.html` ×2, `wiki/faq.html`)
 и заменил на раздельные фразы: ЮMoney — "отключён навсегда" (без
 намёка на возврат), Stars — упомянут как рабочий способ (в боте).
 - `wiki/payment.html`: notice-блок переписан с "ЮMoney и Stars временно
 отключены, могут вернуться из админки" на "ЮMoney отключён навсегда,
 окончательное решение". Добавил третий `flow-row` с описанием Stars
 (только в боте — XTR-платежи технически не существуют вне клиента
 Telegram, сайтового `/api/invoice/stars` никогда не было). В таблице
 "В боте / На сайте" Stars стал `Да` / `—` (было `Отключено`/
 `Отключено`), ЮMoney — "Отключено навсегда" в обеих колонках.
 - `terms.html`/`privacy.html` не трогал — там формулировки и так были
 условными ("когда включены"), не ложными, проверил явно перед тем как
 решить не редактировать.
 - Обновил ADR.md (новый ADR-014) и CLAUDE.md (переписал абзац
 "Payment methods" под ADR-014).
- **Затронутые файлы/папки:** `bot/utils/settings_store.py`,
 `landing/index.html`, `landing/tariffs.html`, `landing/wiki/faq.html`,
 `landing/wiki/payment.html`, `AGENTS/decisions/ADR.md`, `CLAUDE.md`.
- **Важно для следующего агента:** не смешивай "тумблер временно выключен"
 и "мы отказались от способа навсегда" в одной фразе на сайте — это
 разная семантика, и пользователь явно попросил их не путать. Telegram
 Stars (XTR) принципиально работает только внутри клиента Telegram —
 это ограничение Bot Payments API, а не тумблер; не пытайся добавить
 сайтовый чекаут для Stars, это архитектурно невозможно.

### 2026-08-30 — /account: добавил обычное меню (ссылки, гамбургер, "2 дня бесплатно")

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** `account.html` был единственной страницей с урезанной
 шапкой — только лого и пилюля аккаунта, без ссылок меню, без
 гамбургера на мобильном и без кнопки "2 дня бесплатно". Пользователь
 попросил обычное меню, как везде, и спросил, где кнопка.
 - Добавил `.nav-links` (Преимущества/Сравнение/Устройства/Вопросы/
 Тарифы/Wiki, ведут на `/#...`-якоря и `/tariffs`/`/wiki`),
 `.nav-hamburger` + `.nav-menu` для мобильного, `.nav-cta` "2 дня
 бесплатно" (простая ссылка на `@starisvpnbot`, без JS-модалки
 выбора тг/аккаунт — на странице личного кабинета пользователь уже
 всегда авторизован, модалка выбора канала тут не имеет смысла).
 - Существующий дропдаун аккаунта (`#accountBtn`/`#accountDropdown`,
 переключает вкладки через `data-goto`, а не ссылки на `/account`)
 не трогал — просто обернул в новый `.nav-right` рядом с гамбургером,
 сохранив все id и JS-обработчики (`initAccountMenu()`).
 - Убрал `<span class="nav-tag">Личный кабинет</span>` — с полноценным
 меню он был лишним и никуда не помещался по флексу.
 - Сразу применил уже отработанный на 16 других страницах порядок для
 десктопа (`@media(min-width:769px){.nav-actions{display:contents}
 .nav-cta{order:3}}` + `.nav-right{order:2}`) — пилюля аккаунта перед
 кнопкой, кнопка справа.
 - Проверил Playwright (мок `/api/me`): десктоп — `лого → ссылки меню
 → пилюля аккаунта → "2 дня бесплатно"`; мобильный (390px) — гамбургер
 открывает `.nav-menu` с видимыми ссылками, дропдаун аккаунта
 по-прежнему открывается по клику на `#accountBtn` — обе функции
 работают одновременно, не мешая друг другу.
- **Затронутые файлы/папки:** `landing/account.html`.
- **Важно для следующего агента:** на этой странице `.account-menu-wrap`
 — НЕ то же самое, что `#account-btn`/`#account-toggle` на остальных
 страницах (тот генерический дропдаун ведёт на `/account` через href,
 этот же переключает вкладки внутри SPA через `data-goto`) — не путай
 их и не пытайся унифицировать разметку между `account.html` и
 остальными страницами, они намеренно разные.

### 2026-08-30 — ЮMoney удалён полностью: код, конфиг, админка, весь копирайт (ADR-015)

- **Роль:** backend + frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** пользователь потребовал убрать вообще ЛЮБОЕ упоминание
 ЮMoney отовсюду — это прочитано как полное удаление провайдера из
 кода, а не просто ещё одна правка текста поверх ADR-014.
 - Удалил файлы `bot/handlers/yoomoney_payment.py` и
 `bot/utils/yoomoney.py` (`git rm`), убрал роутер из `bot/main.py`.
 - В `bot/api.py` удалил `GET /api/yoomoney/plans`, `POST
 /api/invoice/yoomoney`, `POST /yoomoney/webhook` целиком; упростил
 `/api/gift/invoice` (был `if provider == "card": ... else: # yoomoney
 ...` — стал только card-ветка), `/api/guest/checkout` (был
 `provider in ("robokassa","yoomoney")` с выбором эндпоинта — стал
 только Robokassa), `/api/guest/plans`, `/api/guest/providers`,
 `/web/settings/overview`.
 - `bot/utils/settings_store.py`: `PROVIDER_KEYS`/`_DEFAULT_ENABLED`
 теперь только `card`/`crypto`/`stars` — три ключа вместо четырёх.
 - `bot/handlers/profile.py`: убрал кнопку "🟣 ЮMoney (₽)" из клавиатуры
 выбора способа оплаты.
 - `bot/config.py`/`.env.example`: убрал `YOOMONEY_WALLET`/
 `YOOMONEY_NOTIFICATION_SECRET`.
 - `admin/index.html`: убрал тумблер "ЮMoney" из "Приём оплаты" и
 запись в `methodChip` (таблица платежей) — старые записи с
 `payment_method="yoomoney"` теперь просто показывают нейтральный
 чип "—" (fallback уже был в коде: `methodChip[p.method] || ['—',
 'grey']`, ничего чинить не пришлось).
 - `landing/account.html`: это оказалось не только про текст — вкладки
 "Продление" и "Подарить" реально дёргали `/api/yoomoney/plans`,
 который я только что удалил. Убрал вызов и опцию `yoomoney` из
 обоих провайдер-списков (Promise.all стал двух-, а не трёхэлементным),
 проверил Playwright с моками `/api/card/plans`+`/api/crypto/plans`,
 что обе вкладки рендерятся без JS-ошибок и без единого упоминания
 yoomoney в итоговом HTML.
 - Убрал весь текстовый копирайт (не только "отключено", а вообще
 любое упоминание, включая нейтральные "когда доступна" из
 terms.html/privacy.html, которые в прошлый раз намеренно оставил):
 `index.html`, `tariffs.html` (убрал целую `pay-card`-плашку ЮMoney,
 на её место поставил плашку Telegram Stars, которая внутри бота
 реально работает и раньше нигде на этой странице не упоминалась),
 `terms.html`, `privacy.html` (два места), `wiki/faq.html`,
 `wiki/payment.html` (notice-блок и строка таблицы), `wiki/index.html`
 (поисковый `data-kw`), `card-fail.html`.
 - Прошёлся по документации: `CLAUDE.md` (переписал абзац "Payment
 methods" под ADR-015, поправил "Website account" и "Gift by link"),
 `AGENTS/architecture/database.md`, `AGENTS/architecture/api.md`,
 `AGENTS/architecture/purchase-flow.md` (этот файл был вообще
 устаревшим — описывал состояние ADR-010 из более раннего в тот же
 день; переписал целиком под текущую реальность: /get-vpn требует
 регистрацию, card+crypto+stars включены), `AGENTS/roles/backend.md`,
 `AGENTS/roles/bot.md`, `AGENTS/new_agent.md`, `README.md`.
 - Осознанно НЕ тронул: `alembic/versions/0007_gift_payments.py`
 (историческая миграция) и старые строки `Payment.payment_method =
 "yoomoney"` в БД — это данные, а не код.
- **Затронутые файлы/папки:** `bot/main.py`, `bot/api.py`,
 `bot/config.py`, `bot/utils/settings_store.py`,
 `bot/handlers/profile.py`, `bot/handlers/card_payment.py`
 (docstring), `.env.example`, `admin/index.html`, `landing/account.html`,
 `landing/index.html`, `landing/tariffs.html`, `landing/terms.html`,
 `landing/privacy.html`, `landing/wiki/faq.html`,
 `landing/wiki/payment.html`, `landing/wiki/index.html`,
 `landing/card-fail.html`, `CLAUDE.md`, `AGENTS/decisions/ADR.md`
 (новый ADR-015), `AGENTS/architecture/*.md`, `AGENTS/roles/*.md`,
 `AGENTS/new_agent.md`, `README.md`; удалены `bot/handlers/
 yoomoney_payment.py`, `bot/utils/yoomoney.py`.
- **Важно для следующего агента:** это полное удаление, не тумблер —
 не восстанавливай ЮMoney "по аналогии" с card/crypto/stars, глядя на
 старые ADR или git blame. Перед любым похожим "удали всё упоминание
 X" — не забудь проверить не только текст, но и живые API-вызовы с
 фронтенда (я чуть не оставил `account.html` дёргать удалённый
 эндпоинт `/api/yoomoney/plans`, что сломало бы обе вкладки —
 "Продление" и "Подарить" — рантайм-ошибкой). `git grep -i yoomoney`
 по всему репо — быстрый способ проверить, что реально ничего не
 осталось, кроме исторической миграции и записей changelog'а.

### 2026-08-30 — /account: убрал CTA, корона вместо домика, ключ подписки, карточки-статы вместо "Сервера"

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** по скриншоту личного кабинета пользователь попросил
 сразу несколько точечных правок:
 - Убрал `.nav-cta` "2 дня бесплатно" из шапки `/account` целиком
 (вместе с уже неактуальной десктопной `order`-раскладкой под неё) —
 это личный кабинет уже вошедшего пользователя, звать его в бота
 бесплатным пробным периодом бессмысленно.
 - Заменил "домик" на корону во всех трёх местах, где иконка
 "Подписка" встречается (дропдаун шапки, пункт в сайдбаре, нижняя
 мобильная навигация) — простой геометрический SVG-путь короны
 (3 зубца + основание), в стиле остальных stroke-иконок страницы.
 - Кнопки "Продлить"/"Продлить подписку" (их две — в сайдбаре и в
 основной карточке) теперь скрываются, если у аккаунта вообще
 никогда не было подписки (флаг `trialCard` — `!active &&
 !trial_used`, уже существовал в коде для показа "Активировать
 пробный период", просто раньше не использовался для скрытия
 "Продлить"): "продлить" нечего, пока не было ни пробного периода,
 ни покупки.
 - Убрал карточку "Сервер" (Локация/Протокол/Шифрование/Логи
 трафика построчно, включая давно сломанный `srvLoc` с
 `undefined, undefined` — city/country в `/api/me` этой сборки не
 всегда заполнены) и заменил на грид из 4 карточек с иконками:
 Устройства (X из max), Протокол, Шифрование, Zero Logs. "Локацию"
 убрал совсем — она дублировала карточку "Другие локации" чуть ниже
 и была источником undefined-бага.
 - Добавил над этим гридом карточку "Ключ подписки" — QR + сам ключ
 (моноширинным, обрезанным `text-overflow:ellipsis`) + кнопка
 "Копировать ключ", то есть то же самое, что раньше было только в
 модалке по клику на "Ключ / QR" у конкретного устройства, но сразу
 видно на вкладке "Подписка" для последнего добавленного устройства
 (беру устройство с максимальным `created_at` из уже загруженного
 списка `/api/devices`, дальше как обычно дёргаю
 `/api/devices/{id}/link`). Если устройств ещё нет — показываю
 плейсхолдер с кнопкой "Добавить устройство" вместо самой карточки.
 - Заодно убрал `COUNTRY_FLAGS` — константа стала мёртвым кодом
 после удаления `srvLoc`, единственного места, где она
 использовалась.
 - Проверил Playwright в двух состояниях (мок `/api/me` +
 `/api/devices` + `/api/devices/{id}/link`): аккаунт без подписки и
 без устройств — обе кнопки "Продлить" скрыты, карточка ключа
 показывает плейсхолдер; аккаунт с историей подписки (trial_used) и
 двумя устройствами — обе кнопки "Продлить" видны, карточка ключа
 показывает последнее устройство с реальным QR/ключом/кнопкой
 копирования. Также проверил мобильную раскладку (390px) —
 stat-грид складывается в 2 колонки, карточка ключа — в
 вертикальный стек по центру.
- **Затронутые файлы/папки:** `landing/account.html`.
- **Важно для следующего агента:** "последнее устройство" для карточки
 ключа определяется по максимальному `created_at` из ответа
 `GET /api/devices`, а не по порядку массива — не полагайся на
 порядок сортировки бэкенда, если будешь трогать этот код.
 `renderSubKeyCard()` вызывается из `loadDevices()` при каждой
 загрузке списка устройств (включая после добавления/удаления), так
 что карточка ключа всегда синхронизирована со вкладкой "Устройства".

### 2026-08-30 — /account: ссылка вместо ключа, убрал QR, карточки-статы в стиле тарифов

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** по свежему скриншоту карточки "Ключ подписки"
 пользователь попросил ещё три правки:
 - Обе поверхности с ключом (новая карточка на вкладке "Подписка" и
 модалка "Ключ / QR" на вкладке "Устройства") теперь показывают
 `sub_url` (ссылка на `GET /sub/{username}`, оба эндпоинта
 `/api/devices/{id}/link` и `POST /api/devices` уже отдавали это
 поле — просто раньше никто на сайте его не использовал) вместо
 сырого VLESS-ключа. Нашёл готовый образец UX в мини-приложении
 (`webapp/src/sheets/SubscriptionLinkSheet.jsx`) — тот же текст
 "Открой в Happ, v2rayNG..." и та же логика "ссылка, а не ключ",
 просто веб-версия получила ещё и кликабельную кнопку "Открыть
 ссылку" (обычный `<a href>`), которой у мини-аппа нет.
 - Убрал `<img>` с QR-кодом из обеих поверхностей — по факту это была
 просто белая заглушка без реального смысла, раз теперь ссылку можно
 открыть в одно касание.
 - Переименовал `.stat-card`/`.stat-icon`/`.stat-value`/`.stat-label` в
 `.pay-card`/`.pay-top`/`.pay-title`/`.pay-icon`/`.pay-where` —
 скопировал стиль карточек способов оплаты с `tariffs.html`
 (заголовок слева, иконка справа, значение крупным золотым текстом
 снизу) по прямой просьбе "сделай как на тарифах". Заодно это
 устранило случайное совпадение имени класса `.stat-grid` с
 существующим (и вообще не связанным) гридом статистики на вкладке
 "Приглашения" — до этой правки оба грида делили одно имя класса и
 могли конфликтовать по стилям.
 - Кнопку "Ключ / QR" в списке устройств переименовал в "Ссылка".
- **Затронутые файлы/папки:** `landing/account.html`.
- **Важно для следующего агента:** `sub_url` ≠ `link` — `link` это
 сырой VLESS URI (`vless://...`), `sub_url` это HTTPS-ссылка на
 `/sub/{username}`, которую понимают Happ/v2rayNG как "добавить
 подписку по ссылке". Везде, где показываешь пользователю ссылку на
 сайте, используй `sub_url`, а не `link` — это уже устоявшийся паттерн
 (сначала в мини-аппе, теперь и на сайте), `link` остаётся во внутреннем
 API-ответе, но в интерфейс больше нигде не выводится.

### 2026-08-30 — /account: карточки трафика/рефералов, включил звёздный фон, окно-модалка вернулась к 2 кнопкам

- **Роль:** frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** несколько правок подряд по вкладке "Подписка" и
 общему фону:
 - Пользователь попросил окно ссылки на устройстве вернуть к 2 полным
 кнопкам ("Открыть ссылку"/"Скопировать ссылку") — модалка уже
 полноразмерная, компактная иконка там была лишней; она осталась
 только в узкой inline-карточке на вкладке "Подписка", где места
 меньше. Откатил `.modal-linkrow`/иконку в модалке, вернул
 `<button id="copyKeyBtn">`.
 - Иконки и текст в 4 карточках-статах (`.pay-icon`/`.pay-where`)
 перекрасил с золотого на серый/белый (`var(--text2)`/`var(--text)`)
 — золотой зарезервирован под настоящие call-to-action на странице.
 - Попросили заменить "Протокол"+"Шифрование" на скачано/загружено
 (ГБ), а "Логи трафика" — на количество рефералов. Проверил: реального
 разделения трафика на upload/download в проекте НЕТ — Marzban отдаёт
 только один общий `used_traffic`, и в `bot/api.py` для
 `Subscription-Userinfo` заголовка `upload` уже захардкожен в `0`
 именно по этой причине (`f"upload=0; download={used}; ..."`). Раз
 честно показать два разных числа нечем, сделал 3 карточки вместо 4:
 Устройства, Трафик (общий `ME.traffic_gb`), Рефералы
 (`ME.referral_count`) — не стал изобретать фиктивное "upload".
 - На мобильном для 3 карточек (`.pay-grid`) уже был
 `@media(max-width:480px){grid-template-columns:repeat(2,1fr)}` —
 не трогал, третья карточка просто переносится на новую строку.
 - "Сделай фон звёздным" — фон уже существовал (`#bg-canvas`, canvas
 со звёздами, копия того же паттерна что и на `index.html`), но был
 практически невидим: 150 звёзд с opacity 0.08–0.58 плюс ещё общий
 CSS `opacity:.28` на канвасе поверх — итоговая яркость точек была
 околонулевой. Убрал CSS-обёртку opacity, поднял количество звёзд до
 280 и диапазон яркости точек, взяв тюнинг из `index.html`'s
 starfield (280 звёзд, радиус 0.25–1.75, opacity 0.2–0.75). Слой
 остался `z-index:0;pointer-events:none` — на элементы не залезает и
 клики не блокирует, просто теперь заметен в промежутках между
 карточками.
 - Пользователь попросил показать остальные вкладки — сделал
 Playwright-тур по всем 6 (Подписка/Устройства/Продление/
 Приглашения/Подарить/Профиль) с моками API, поймал попутно
 баг в СВОЁМ моке (`ref.achievements.map` падал на undefined —
 не баг сайта, просто забыл поле в тестовых данных), поправил и
 переснял.
- **Затронутые файлы/папки:** `landing/account.html`.
- **Важно для следующего агента:** если когда-нибудь понадобится
 честный upload/download сплит по трафику — это не "поправить в
 JS", это требует новой интеграции на уровне Xray/Marzban stats API
 (текущий Marzban-клиент, `bot/utils/marzban.py`, вообще не имеет
 метода для постатейной статистики), не пытайся сделать это как
 мелкую фронтенд-правку.

### 2026-08-30 — /account: одна кнопка добавления устройства, свой срок продления, богаче "Подарить"/"Профиль", предложение по рефералам

- **Роль:** backend + frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:** пользователь прислал 6 скриншотов с кружками и 5
 отдельных пунктов правок:
 1. **Устройства**: грид из 7 кнопок "добавить устройство" (по одной
 на платформу) заменил одной кнопкой "+ Добавить устройство",
 открывающей модалку-пикер с теми же иконками (переиспользуется
 `.modal-overlay`/`.modal` — на мобильном автоматически становится
 bottom sheet, эта механика уже была). Бейдж "2/3" в сайдбаре
 уменьшил и прижал к правому краю строки (`margin-left:auto`)
 вместо мелкого монопространственного блока сразу после текста.
 2. **Продление**: добавил произвольный срок (7–180 дней) для оплаты
 картой — слайдер + быстрые кнопки 30/90/180, тот же тариф за день,
 что и на `tariffs.html` (7₽/6.6₽/5.5₽/5₽ по порогам), формула
 продублирована в JS один в один с `tariffs.html`'s `rateFor/priceFor`
 для совпадения превью с реальным списанием. На бэкенде
 `POST /api/invoice/card` научил принимать `{"days": N}` как
 альтернативу `{"plan": key}` — валидация через
 `robokassa.CUSTOM_DAYS_MIN/MAX`, цена через уже существующий
 `custom_plan_price()` (тот же, что использует гостевой чекаут).
 Для крипты произвольный срок НЕ добавлял — в проекте нет ни одной
 установленной ставки за день в USD, выдумывать её самому — это
 ценовое решение, не фронтенд-правка. Кнопки способа оплаты
 (карта/крипта) получили иконки.
 3. **Подарить**: было слишком пусто — добавил hero-карточку
 ("Подарите STAR VPN" + иконка подарка) и ряд из 3 карточек-фич
 (без риска / мгновенно / без документов) над формой получателя.
 4. **Профиль**: добавил 3 новые карточки — "Приватность" (текст про
 Zero Logs), "Помощь" (Wiki/FAQ/Поддержка), "Документы" (Условия/
 Конфиденциальность). Специально НЕ стал добавлять кнопку "привязать
 Telegram" и переключатели уведомлений/языка, хотя пользователь
 разрешил "как в мини-приложении" — проверил: `/api/telegram-oauth/
 callback` делает голый get-or-create по telegram_id без какой-либо
 привязки к текущей сессии, так что кнопка "привязать" у уже
 залогиненного email-аккаунта тихо переключила бы его на другой
 (или новый) аккаунт вместо привязки — это баг, а не фича, раз так
 сделать. У самого мини-аппа `notificationToggles`/`languageOptions`
 в `webapp/src/App.jsx` — чисто клиентский `useState`, ничего не
 сохраняется и не влияет на реальные уведомления, так что "как в
 мини-аппе" не значит "эти тумблеры настоящие" — не стал их копировать.
 5. **Реферальная программа**: не трогал код начисления. Пользователь
 попросил именно "напиши файлы, распиши новый принцип" — это описано
 как отдельная задача на согласование, а не команда сразу менять
 код: написал `AGENTS/architecture/referral-system.md` (математика:
 текущая система даёт 585 дней на 25 рефералов — 360 от базового
 бонуса + 225 от разовых достижений `REFERRAL_ACHIEVEMENTS`, что
 больше чем в 3 раза дороже самого дорогого тарифа) и ADR-016 с
 предложением убрать достижения и оставить только базовый бонus
 (30 дней за 2 друга, без потолка) — 360 дней вместо 585. Явно
 пометил статус "Предложено", код `_credit_referral`/
 `REFERRAL_ACHIEVEMENTS` не менял.
 - Заодно нашёл и убрал случайное совпадение имени класса `.stat-grid`
 в CSS с прошлой правки (см. запись про звёздный фон) — не относится
 к этой сессии правок напрямую, упомянуто там же.
 - Проверил Playwright: модалка добавления устройства (десктоп +
 мобильный bottom sheet), слайдер продления (превью цены = серверная
 цена на реальном сабмите, поймал через перехват запроса — 60 дней
 → 396₽ и на клиенте, и в отправленном на сервер JSON), карточки
 "Подарить" и "Профиль" визуально и без JS-ошибок.
- **Затронутые файлы/папки:** `landing/account.html`, `bot/api.py`
 (`POST /api/invoice/card`), `AGENTS/decisions/ADR.md` (новый ADR-016),
 новый файл `AGENTS/architecture/referral-system.md`.
- **Важно для следующего агента:** не внедряй предложение из ADR-016
 без явного "делай" от пользователя — это про юнit-экономику, не
 техдолг. Если всё же будешь внедрять: меняй `REFERRAL_ACHIEVEMENTS`/
 `_credit_referral` в `bot/handlers/payment.py` И `.badge-grid`-блок
 в `landing/account.html::loadReferral()` в одном коммите — бэкенд
 перестанет отдавать `achievements`, и если фронтенд не поправить
 одновременно, `ref.achievements.map` в шаблоне упадёт (для меня же
 самого в этой сессии это уже случалось один раз — я тогда просто
 забыл нужное поле в моковых тестовых данных Playwright, не в
 реальном коде, но механизм падения тот же).

### 2026-08-30 — Имена устройств (двухшаговое добавление + переименование), чище "Продление", проще "Подарить"

- **Роль:** backend + frontend
- **Кто:** Claude (Claude Code)
- **Что сделал:**
 - **Имена устройств.** Раньше `Device.name` хранил только тип
 платформы (`ios`/`windows`/...), несмотря на то что докстринг модели
 обещал "iPhone 15" — реального пользовательского имени не было
 нигде. Добавил `Device.custom_name` (миграция
 `0013_device_custom_name`, nullable — устройства, добавленные через
 бота, его не задают и работают как раньше). `POST /api/devices`
 теперь принимает необязательный `name`; новый `PATCH
 /api/devices/{id}` переименовывает в любой момент. `GET
 /api/devices` и `GET /api/devices/{id}/link` отдают `custom_name`, и
 если оно задано — именно оно идёт в VLESS remark через
 `set_vless_remark_text` (не только на сайте видно новое имя, но и в
 самом Happ/v2rayNG).
 - На сайте флоу добавления устройства стал двухшаговым внутри одной
 модалки: шаг 1 — категория (Телефон/Планшет/Компьютер/Телевизор,
 несколько платформ теперь делят один и тот же тип — iPhone и iPad
 оба создают `ios`-устройство, это нормально, тип определяет только
 конфиг клиента); шаг 2 — поле имени, предзаполненное лейблом
 нажатой кнопки (не общим `DEVICE_LABELS[type]`, а именно текстом
 конкретной кнопки — иначе iPhone и iPad показывали бы одинаковое
 "iPhone / iPad" в поле имени). У каждой строки устройства появилась
 иконка-карандаш — переименование доступно всегда, не только при
 создании; шёл через `DEVICES_CACHE` (заполняется в `loadDevices()`)
 вместо протаскивания имени через `data-*` атрибут, чтобы не думать
 об экранировании кавычек в пользовательском тексте.
 - **Продление** пользователь назвал "компактным и неприятным" после
 прошлой правки — обернул способ оплаты + срок в один `.card` с
 подписанными секциями ("Способ оплаты" / "Срок подписки") вместо
 голых стопки тумблеров.
 - **Подарить**: тот же пользователь, что просил "не пусто" в
 прошлый раз, теперь сказал "слишком много информации" — убрал грид
 из 3 карточек-фич (без риска/мгновенно/без документов), оставил
 только однострочный hero без текста-простыни.
 - Проверил Playwright: полный цикл добавления устройства с именем
 (перехватил `POST /api/devices`, тело содержит `{"type":...,
 "name":...}`), переименование существующего (`PATCH
 /api/devices/{id}` с правильным телом), новый вид "Продление" и
 "Подарить" на десктопе, rename-модалка на мобильном (bottom sheet).
- **Затронутые файлы/папки:** `bot/models/device.py`, `bot/api.py`
 (`POST`/`PATCH`/`GET /api/devices*`), новая миграция
 `alembic/versions/0013_device_custom_name.py`,
 `landing/account.html`, `CLAUDE.md` (новый абзац "Device names").
- **Важно для следующего агента:** `bot/handlers/devices.py` (бот-флоу
 добавления устройств) НЕ трогал — он создаёт `Device` без
 `custom_name`, что абсолютно нормально (nullable, есть фоллбэк).
 Если попросят переименование и из бота — там своя копия логики
 создания устройства, придётся добавлять отдельно, это не то же
 самое место в коде.
